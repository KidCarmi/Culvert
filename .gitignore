# Compiled binaries
proxy
culvert
updater/culvert-updater

# Local configuration (may contain real passwords — never commit)
config.yaml

# CA bundles contain private keys
*.bundle

# Cluster CA files (generated at runtime)
cluster-ca.crt
cluster-ca.key

# Data Plane enrollment files
dp-node.crt
dp-node.key
dp_enrollment.json

# HA runtime files
cp-leader.lock
ha_config.json

# Sensitive runtime state (these live in /data volume, not in git)
data/

# Atomic-write temp files
*.tmp

# Environment files
.env
.env.*

# Editor cruft
.DS_Store
coverage.out
cover.out
