# Trivy suppression list — intentionally empty.
#
# The former entries masked docker/docker SDK CVEs reachable only through the
# legacy culvert-updater sidecar module, accepted under RISK-ACC-1 / DEBT-008.
# That module has been removed, so nothing in the tree imports docker/docker
# and the masks are no longer needed. Keep this file present (workflows pass
# `--ignorefile .trivyignore`); add new suppressions here with a dated `exp:`
# and a rationale.
