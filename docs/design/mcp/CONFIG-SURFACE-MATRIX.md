# MCP Configuration Surface Matrix

**Status: PR-0 design artifact (Proposed).** Every field in this document is a **PROPOSED** design
field for the two MCP capabilities described in [`BLUEPRINT.md`](BLUEPRINT.md) and
[`RECOMMENDED-ARCHITECTURE.md`](RECOMMENDED-ARCHITECTURE.md). None of these fields exist in the
repository today (**[FACT]** grep for an MCP/JSON-RPC listener returns zero hits — see
[`VERIFIED-REPOSITORY-CONTEXT.md`](VERIFIED-REPOSITORY-CONTEXT.md)). Nothing in this document should be
read as describing shipped behavior.

## Purpose

This matrix exists so that **no MCP configuration field is ever proposed, reviewed, or implemented
without a complete cross-surface plan.** The repository's GUI-parity mandate
(`CLAUDE.md`:123 — *"Every new CLI flag or config option MUST have a corresponding admin API endpoint
AND a UI panel/section so the user can manage it from the GUI. CLI-only features are not
acceptable"*) already forbids CLI-only configuration for the existing Secure Web Gateway (SWG)
product. This document extends that mandate, field-by-field, to the two proposed MCP capabilities and
adds the additional surfaces MCP introduces that SWG config never had to reason about (durable
decision-event pipelines, CP→DP catalog/credential revisions, per-tenant overrides, secret-adjacent
credential-profile metadata).

**A PR that introduces a new MCP config field without a corresponding row in this matrix — and,
starting at PR-1, a corresponding row in the real `configSurfaces` registry described below — is
incomplete.** The purpose of a design-time matrix is to make that incompleteness visible in review,
before the field exists in code where an anti-drift test would otherwise have to catch it.

## The anti-drift pattern this surface must follow

**[FACT]** Culvert already solved this exact problem once, for the SWG product, and the solution is a
hard architectural precedent MCP configuration MUST reuse rather than reinvent:

- `config_surfaces.go` (`configSurfaces`, lines 104–455) is a **declarative registry** — one
  `configSurfaceRow` per logical setting — that states, for every field, which of five surfaces it
  lives on and with what semantics:
  1. **Export/import** (`apiConfigExport`/`apiConfigImport`) — the full backup/restore surface.
  2. **Version-rollback** (`captureConfigBackup`/`applyConfigBackup`/`diffConfigs` in
     `configversion.go`) — a curated subset with per-field nil-skip/`[]`-wipe semantics distinct from
     export/import.
  3. **Restart durability** (`/data/admin_settings.json`, gated by `*Saved` sentinel booleans).
  4. **CP→DP sync** (`ConfigSnapshot`, `controlplane_snapshot.go`) — pushed to enrolled nodes, subject
     to `validateConfigSnapshot` DoS caps.
  5. **Snapshot-cap validation** — the per-slice/map size bound enforced before a snapshot is ever
     applied.
- The registry is **enforced, not aspirational**: `config_surfaces_test.go` reflection-walks the real
  `configBackup`, `AdminSettings`, and `ConfigSnapshot` structs and fails the build if any struct field
  is unclaimed by a registry row, or if a registry row's binding doesn't resolve to a real field
  (forward **and** reverse parity — the same two-directional pattern as the `uiRoutes`/C1 layer for
  admin API routes). Adding a config field means: add the struct field, add or extend a registry row,
  and the tests enumerate every remaining place the field still needs to be wired.

**MCP config MUST follow this pattern, not bypass it.** Concretely, when MCP configuration is
implemented starting at PR-1/PR-2:

- Every field in this matrix that reaches `Admin API` = Yes and `GUI panel` = Yes eventually needs a
  `configBackup`/`AdminSettings`/`ConfigSnapshot` struct field (as applicable) and a matching
  `configSurfaces` row. **Skipping the registry row is not an option that passes CI** — the parity
  tests are reflection-based, not opt-in.
- MCP introduces surfaces the existing five don't fully cover (durable decision-event export target,
  per-tenant override maps, credential-profile *metadata* propagation). Where an MCP field doesn't map
  cleanly onto one of the five existing surfaces, PR-1/PR-2 MUST either extend `config_surfaces.go`'s
  taxonomy (a new `fieldKind`/surface column) or introduce a parallel MCP-specific registry with the
  same enforced-parity property — never an unregistered ad hoc field. This decision is intentionally
  left open; see [`OPEN-DECISIONS.md`](OPEN-DECISIONS.md).
- The two MCP capabilities keep **separate registry rows and, if a parallel registry is chosen,
  separate registries** — a Management MCP field and a Gateway MCP field are never the same row, even
  when conceptually parallel (e.g. "listener bind address" exists twice, never once, shared).

## Doctrine reminder (do not merge the two capabilities)

Per [`BLUEPRINT.md`](BLUEPRINT.md) and [`RECOMMENDED-ARCHITECTURE.md`](RECOMMENDED-ARCHITECTURE.md),
"one Culvert platform; shared Control Plane services; SEPARATE enforcement engines and trust
boundaries":

- **Capability A — Culvert Management MCP Server** (`mcp_mgmt` namespace): `AI Client →
  /mcp/management → Culvert management`. Read-only by default; tenant-scoped; RBAC-gated; redacted;
  rate-limited; audited. No raw secrets, no command execution, no unrestricted log export, no
  production mutation until a plan→validate→approve→apply control exists (**MCP-MGMT-001**).
- **Capability B — MCP Security Gateway** (`mcp_gateway` namespace): `AI Agent → /mcp/gateway/{server-id}
  → Approved MCP Server → Enterprise System`. Owns identity, tool discovery/fingerprinting, policy
  evaluation, inspection, credential brokering, enforcement, and durable decision events.

Every row below is tagged `Capability = Mgmt` or `Capability = Gateway`. No row is shared. Where a
capability legitimately has **no** field for a requested category (e.g. Management MCP has no
outbound credential broker because it never touches upstream enterprise credentials), that is recorded
as an explicit `N/A` row rather than silently omitted, so the matrix is auditable against the full
category checklist.

## Column legend

| Column | Meaning |
|---|---|
| File config (YAML) | Proposed key path under a new top-level `mcp:` YAML block. |
| Env var | Proposed environment-variable override, following the `CULVERT_<DOMAIN>_<NAME>` convention (`CULVERT_C2_ENFORCE`, `CULVERT_CLUSTER_GRPC_COMPRESSION`, `CULVERT_RELEASE_CATALOG_VERIFY`, etc.). |
| CLI flag | Proposed `-mcp-...` flag, following the existing dash-lowercase convention (`-ha-etcd-endpoints`, `-cluster-insecure`). |
| Admin API | Proposed `apiXxx` handler + route, registered via a `register*Routes` helper and represented in `uiRoutes` (per the existing admin-API pattern). `requireRole` shown where the default RBAC floor differs from the route's own `MinRole`. |
| GUI panel | Proposed SPA panel/section (`data-view="mcp-..."`) and its nav placement. |
| OpenAPI | Whether the endpoint is proposed for `api/openapi/openapi.yaml`, subject to the existing `api-contract.yml`/`pr-api-governance.yml` breaking-change + client-gen merge-blocking gates. |
| Validation | The proposed input-validation rule (bounds, enum, format). |
| Documentation | The sibling design doc that is the authoritative spec for this field's *behavior* (this matrix owns only the *surface plan*). |
| Tests | Proposed test file(s), following the existing parity-test naming convention (`config_surfaces_test.go`, `ui_routes_meta_test.go`), and the PR in the [implementation sequence](#implementation-sequence-reference) that introduces them. |
| Snapshot publication (CP→DP) | Whether the field is proposed for `ConfigSnapshot` (CP→DP sync), and its `validateConfigSnapshot` cap if it is a slice/map. |
| Backward compatibility | See [Backward-compatibility convention](#backward-compatibility-convention) below — `Std` unless overridden inline. |
| Secret handling | `None` \| `Metadata-only` (profile IDs/scope, never the secret) \| `N/A`. See [Secret handling](#secret-handling) below. |

### Backward-compatibility convention

Unless a row overrides it: **every field is additive, carries an `omitempty` JSON tag on the wire
struct, and an older binary that doesn't recognize the field ignores it and falls back to its
pre-existing (safe) behavior.** This directly mirrors the repo's existing precedent for
`decryptprofile.OnInspectError`: *"an additive persisted profile field (`omitempty`) that degrades to
fail-close on downgrade (old binary ignores the unknown key)"* (`CLAUDE.md`, Adaptive decryption
exclusion). For MCP this means: a downgraded/older Gateway node that doesn't understand a new
inspection-limit or policy field must fail closed (deny/quarantine), never silently widen behavior —
the same asymmetry `MCP-CRED-006`'s broker failure matrix already requires for credentials. Rows where
the safe direction on downgrade is anything other than "ignore and keep prior safe default" say so
explicitly in their `Backward compatibility` cell (marked `Override`).

### Secret handling

Per **MCP-CRED-004** (*"Secrets MUST NOT appear in logs, metrics, traces, errors or decision
events"*) and **MCP-EVENT-003** (*"Events MUST NOT store raw tokens, upstream secrets, private keys,
complete raw arguments or complete raw outputs by default"*), **no field in this matrix carries secret
material.** Credential-profile fields carry only profile IDs, environment/server/tool-class/
resource-scope metadata, and provider references (Vault path, KMS key ID, workload-identity binding) —
never the resolved secret value, never a ciphertext blob synced over `ConfigSnapshot`. This mirrors the
existing durability precedent: `/data/admin_settings.json` is `0600`, and where secret-adjacent
material genuinely must persist, `internal/secret`'s KEK/provider containment model
(`ADR-0007-secret`, cited as reusable prior art in
[`AUTH-AND-CREDENTIAL-MODEL.md`](AUTH-AND-CREDENTIAL-MODEL.md) §7.2) is the intended containment layer
— not a new ad hoc encryption scheme. `internal/redaction` (fail-closed `DataClass`) is the intended
reuse target for output/event redaction profiles referenced below.

### Implementation sequence reference

`PR-1` Protocol kernel · `PR-2` Registry & catalog · `PR-3` Identity principal · `PR-4` Credential
broker · `PR-5` Observe runtime · `PR-6` Policy engine · `PR-7` Inspection · `PR-8` Durable decision
events · `PR-9` API & GUI · `PR-10` CP/DP & HA · `PR-11` Shadow & canary · then a separate
**Production Qualification** gate (no PR-12).

---

## The matrix

| Field ID | Capability | Category | File config (YAML) | Env var | CLI flag | Admin API | GUI panel | OpenAPI | Validation | Documentation | Tests | Snapshot (CP→DP) | Backward compat | Secret handling |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `mcp_mgmt_enabled` | Mgmt | Enable/disable | `mcp.management.enabled` | `CULVERT_MCP_MGMT_ENABLED` | `-mcp-mgmt-enabled` | `GET/PUT /api/mcp/management/settings` (admin write, viewer read) | MCP → Management → General (`data-view="mcp-management"`) | Yes | bool | [BLUEPRINT.md](BLUEPRINT.md) §9 | `mcp_mgmt_startup_test.go` (PR-9) | No — Mgmt runs CP-side only (see `mcp_mgmt_config_sync_enabled`) | Std — off by default; unknown key ⇒ disabled | None |
| `mcp_mgmt_listen_bind` | Mgmt | Listener bind/port | `mcp.management.listen_bind` | `CULVERT_MCP_MGMT_LISTEN_BIND` | `-mcp-mgmt-listen-bind` | `GET/PUT /api/mcp/management/settings` | MCP → Management → General | Yes | valid IP/hostname; rejects `0.0.0.0` without an explicit ack flag | [BLUEPRINT.md](BLUEPRINT.md) §9, [RECOMMENDED-ARCHITECTURE.md](RECOMMENDED-ARCHITECTURE.md) §4 | `mcp_mgmt_listener_test.go` (PR-5/PR-9 — listener binding is PR-5, MCP-INSP-009) | No | Std | None |
| `mcp_mgmt_listen_port` | Mgmt | Listener bind/port | `mcp.management.listen_port` | `CULVERT_MCP_MGMT_LISTEN_PORT` | `-mcp-mgmt-listen-port` | `GET/PUT /api/mcp/management/settings` | MCP → Management → General | Yes | `1–65535`; MUST NOT equal `mcp_gateway_listen_port` (separate-listener invariant) | [RECOMMENDED-ARCHITECTURE.md](RECOMMENDED-ARCHITECTURE.md) §1 (separate runtimes) | pure config validation that the two ports differ `mcp_listener_port_config_test.go` (**PR-9** — when the settings fields/API become real) + runtime listener separation `mcp_listener_separation_test.go` (**PR-5** — actual bound sockets); admin API/GUI/OpenAPI parity **PR-9**. **PR-1 binds no listener.** | No | Std | None |
| `mcp_mgmt_protocol_version_policy` | Mgmt | Protocol version policy | `mcp.management.protocol_versions` | `CULVERT_MCP_MGMT_PROTOCOL_VERSIONS` | `-mcp-mgmt-protocol-versions` | `GET/PUT /api/mcp/management/settings` | MCP → Management → Protocol | Yes | non-empty allow-list of `[EXT]` MCP protocol version strings; unknown-version negotiation MUST reject, never best-effort-accept | [PROTOCOL-COMPATIBILITY.md](PROTOCOL-COMPATIBILITY.md) §2, §9 | `mcp_mgmt_protocol_negotiation_test.go` (PR-1) | No | Override — a downgraded node that drops a version from its own allow-list MUST reject clients requesting it, not silently accept | None |
| `mcp_mgmt_auth_mode` | Mgmt | Identity/auth | `mcp.management.auth_mode` | `CULVERT_MCP_MGMT_AUTH_MODE` | `-mcp-mgmt-auth-mode` | `GET/PUT /api/mcp/management/settings` | MCP → Management → Auth | Yes | enum `session-cookie` \| `oauth-token`; V1 default `session-cookie` (reuses existing RBAC session, no new token issuer) | [AUTH-AND-CREDENTIAL-MODEL.md](AUTH-AND-CREDENTIAL-MODEL.md) §2–§4 | `mcp_mgmt_auth_test.go` (PR-3) | No | Std | None |
| `mcp_mgmt_default_min_role` | Mgmt | RBAC | `mcp.management.default_min_role` | `CULVERT_MCP_MGMT_DEFAULT_MIN_ROLE` | `-mcp-mgmt-default-min-role` | `GET/PUT /api/mcp/management/settings` | MCP → Management → Auth | Yes | enum `viewer` \| `operator` \| `admin`; MUST NOT be lower than `viewer` | [SECURITY-REQUIREMENTS.md](SECURITY-REQUIREMENTS.md) MCP-MGMT-002 | `mcp_mgmt_rbac_test.go` (PR-9) | No | Std | None |
| `mcp_mgmt_tool_allowlist` | Mgmt | Tool catalog/approval | `mcp.management.tools` (list of tool IDs + enabled bool) | n/a (list; no env-var equivalent) | n/a (list) | `GET/PUT /api/mcp/management/tools` (admin write, viewer read) | MCP → Management → Tools | Yes | tool IDs MUST be drawn from the fixed built-in management-tool set (no dynamic registration — Management has no server registry, unlike Gateway) | [SECURITY-REQUIREMENTS.md](SECURITY-REQUIREMENTS.md) MCP-MGMT-002/003 | `mcp_mgmt_tools_test.go` (PR-9) | No — Mgmt tool set is CP-local, not DP-synced | Std — unknown tool ID in an older binary is ignored, not fatal | None |
| `mcp_mgmt_mutation_enabled` | Mgmt | Enable/disable (mutation gate) | `mcp.management.mutation_enabled` | `CULVERT_MCP_MGMT_MUTATION_ENABLED` | `-mcp-mgmt-mutation-enabled` | `GET/PUT /api/mcp/management/settings` (admin-only write) | MCP → Management → General (danger-zone toggle, admin-only) | Yes | bool; **hard-fails validation to `false`** until a plan→validate→approve→apply control ships (no PR ships this as `true`-capable before that control exists) | [SECURITY-REQUIREMENTS.md](SECURITY-REQUIREMENTS.md) MCP-MGMT-001 | `mcp_mgmt_mutation_gate_test.go` (PR-9, Prod-Qual) | No | Override — MUST default to and remain `false` on any downgrade/upgrade; never inferred `true` | None |
| `mcp_mgmt_tenant_scope_mode` | Mgmt | Per-tenant settings | `mcp.management.tenant_scope_mode` | `CULVERT_MCP_MGMT_TENANT_SCOPE_MODE` | `-mcp-mgmt-tenant-scope-mode` | `GET/PUT /api/mcp/management/settings` | MCP → Management → Tenancy | Yes | enum `session-bound` (V1 only — tenant is derived from the authenticated admin session, never client-asserted) | [SECURITY-REQUIREMENTS.md](SECURITY-REQUIREMENTS.md) MCP-ID §cross-tenant (MCP-T-009/010) | `mcp_mgmt_tenancy_test.go` (PR-3) | No | Std | None |
| `mcp_mgmt_output_max_bytes` | Mgmt | Inspection limits | `mcp.management.output_max_bytes` | `CULVERT_MCP_MGMT_OUTPUT_MAX_BYTES` | `-mcp-mgmt-output-max-bytes` | `GET/PUT /api/mcp/management/settings` | MCP → Management → Limits | Yes | positive int; ceiling constant caps operator input (mirrors `snapshotCapCeiling` pattern) | [SECURITY-REQUIREMENTS.md](SECURITY-REQUIREMENTS.md) MCP-MGMT-004 | `mcp_mgmt_output_bound_test.go` (PR-7/PR-9) | No | Std | None |
| `mcp_mgmt_output_redaction_profile` | Mgmt | Inspection limits | `mcp.management.redaction_profile` | `CULVERT_MCP_MGMT_REDACTION_PROFILE` | `-mcp-mgmt-redaction-profile` | `GET/PUT /api/mcp/management/settings` | MCP → Management → Limits | Yes | references an `internal/redaction` `DataClass` profile ID; unknown ID rejected at validation, never silently no-op | [SECURITY-REQUIREMENTS.md](SECURITY-REQUIREMENTS.md) MCP-MGMT-004 | `mcp_mgmt_redaction_test.go` (PR-7) | No | Std | None |
| `mcp_mgmt_rate_limit_rps` / `mcp_mgmt_rate_limit_burst` | Mgmt | Availability | `mcp.management.rate_limit.{rps,burst}` | `CULVERT_MCP_MGMT_RATE_LIMIT_RPS` / `_BURST` | `-mcp-mgmt-rate-limit-rps` / `-mcp-mgmt-rate-limit-burst` | `GET/PUT /api/mcp/management/settings` | MCP → Management → Limits | Yes | positive int; `burst ≥ rps` | [SECURITY-REQUIREMENTS.md](SECURITY-REQUIREMENTS.md) MCP-OPS-002 | `mcp_mgmt_ratelimit_test.go` (PR-5) | No | Std | None |
| `mcp_mgmt_config_sync_enabled` | Mgmt | CP/DP snapshot toggle | `mcp.management.cp_dp_sync_enabled` | `CULVERT_MCP_MGMT_CPDP_SYNC_ENABLED` | `-mcp-mgmt-cp-dp-sync-enabled` | `GET/PUT /api/mcp/management/settings` | MCP → Management → General (advanced) | Yes | bool; **default `false`** — Management MCP is a Control-Plane-local admin surface by design ([RECOMMENDED-ARCHITECTURE.md](RECOMMENDED-ARCHITECTURE.md) §1); this field exists only to document that a future multi-CP topology could need it, not because a DP consumes Management config today | [CP-DP-HA-MODEL.md](CP-DP-HA-MODEL.md) §1 | `mcp_mgmt_cpdp_test.go` (PR-10, if ever exercised) | Reserved, not wired | Std | None |
| `mcp_mgmt_credential_profiles` | Mgmt | Credential-profile metadata | — **N/A** | — | — | — | — | — | — | [AUTH-AND-CREDENTIAL-MODEL.md](AUTH-AND-CREDENTIAL-MODEL.md) §7 | — | N/A | N/A | **N/A by design** — Management MCP never brokers upstream enterprise credentials; it acts as the authenticated admin's own RBAC session against Culvert's own management API (`mcp_mgmt_auth_mode`), so no credential-profile registry exists on this capability. Recorded explicitly so the category checklist is auditably complete rather than silently skipped. |
| `mcp_mgmt_connector_mode` | Mgmt | Connector/DMZ config | — **N/A** | — | — | — | — | — | — | [ON-PREM-CONNECTIVITY.md](ON-PREM-CONNECTIVITY.md) | — | N/A | N/A | **N/A by design** — Management MCP is inbound-only (`AI Client → /mcp/management → Culvert`); it has no outbound leg to an enterprise system and therefore no connector/DMZ model. That topology exists only for Capability B (`mcp_gateway_connector_mode` below). |
| `mcp_gateway_enabled` | Gateway | Enable/disable | `mcp.gateway.enabled` | `CULVERT_MCP_GW_ENABLED` | `-mcp-gateway-enabled` | `GET/PUT /api/mcp/gateway/settings` (admin write, viewer read) | MCP → Gateway → General (`data-view="mcp-gateway"`) | Yes | bool | [BLUEPRINT.md](BLUEPRINT.md) §1 | `mcp_gateway_startup_test.go` (PR-9) | Yes — meta bool, no cap | Std — off by default; unknown key ⇒ disabled; MCP-OPS-001 requires zero SWG regression when off | None |
| `mcp_gateway_listen_bind` | Gateway | Listener bind/port | `mcp.gateway.listen_bind` | `CULVERT_MCP_GW_LISTEN_BIND` | `-mcp-gateway-listen-bind` | `GET/PUT /api/mcp/gateway/settings` | MCP → Gateway → General | Yes | valid IP/hostname | [RECOMMENDED-ARCHITECTURE.md](RECOMMENDED-ARCHITECTURE.md) §4 | `mcp_gateway_listener_test.go` (PR-5 — listener binding is PR-5, MCP-INSP-009; PR-1 ships no listener) | No — listener bind is node-local, not CP-published | Std | None |
| `mcp_gateway_listen_port` | Gateway | Listener bind/port | `mcp.gateway.listen_port` | `CULVERT_MCP_GW_LISTEN_PORT` | `-mcp-gateway-listen-port` | `GET/PUT /api/mcp/gateway/settings` | MCP → Gateway → General | Yes | `1–65535`; MUST NOT equal `mcp_mgmt_listen_port` | [RECOMMENDED-ARCHITECTURE.md](RECOMMENDED-ARCHITECTURE.md) §1 | pure config validation that the two ports differ `mcp_listener_port_config_test.go` (**PR-9** — when the settings fields/API become real) + runtime listener separation `mcp_listener_separation_test.go` (**PR-5** — actual bound sockets); admin API/GUI/OpenAPI parity **PR-9**. **PR-1 binds no listener.** | No | Std | None |
| `mcp_gateway_protocol_version_policy` | Gateway | Protocol version policy | `mcp.gateway.protocol_versions` | `CULVERT_MCP_GW_PROTOCOL_VERSIONS` | `-mcp-gateway-protocol-versions` | `GET/PUT /api/mcp/gateway/settings` | MCP → Gateway → Protocol | Yes | non-empty allow-list of `[EXT]` MCP protocol versions; downgrade-negotiation policy per [PROTOCOL-COMPATIBILITY.md](PROTOCOL-COMPATIBILITY.md) §9 | [PROTOCOL-COMPATIBILITY.md](PROTOCOL-COMPATIBILITY.md) §2, §9 | `mcp_gateway_protocol_negotiation_test.go` (PR-1) | Yes — meta string list, capped | Override — a version dropped from policy MUST reject in-flight negotiation, never silently downgrade to an unlisted version | None |
| `mcp_gateway_origin_host_allowlist` | Gateway | Listener bind/port (inbound rebinding defense) | `mcp.gateway.origin_host_allowlist` | n/a (list) | n/a (list) | `GET/PUT /api/mcp/gateway/settings` | MCP → Gateway → General | Yes | non-empty list of expected `Origin`/`Host` values for the inbound SSE/MCP listener; **fail-closed** — a request with no match is rejected, mirroring the SSRF `PrivateHost` fail-closed posture (`internal/ssrf`) but for the inbound direction, which the repo does not currently guard (`isSafeRedirectURL` is captive-portal-only) | [SECURITY-REQUIREMENTS.md](SECURITY-REQUIREMENTS.md) MCP-INSP-008 (primitive) / MCP-INSP-009 (listener), [ON-PREM-CONNECTIVITY.md](ON-PREM-CONNECTIVITY.md) §7 | `mcp_gateway_origin_primitive_test.go` (PR-1 — allowlist consumed by the pure primitive) + `mcp_gateway_origin_rebinding_e2e_test.go` (PR-5 — listener E2E) | No — node-local listener hardening | Override — an empty/missing allow-list MUST fail closed (reject all), never fail open to "no check" | None |
| `mcp_gateway_server_registry` | Gateway | Server registry entries | `mcp.gateway.servers` (list: id, transport, endpoint, expected fingerprint) | n/a (list) | n/a (list) | `GET/POST/PUT/DELETE /api/mcp/gateway/servers[/{id}]` (admin write, viewer read) | MCP → Gateway → Servers | Yes | endpoint MUST pass the same SSRF/private-IP checks as any outbound dial (`isPrivateHost()` inline per `CLAUDE.md` convention); fingerprint required before a server transitions to approved | [TOOL-DISCOVERY-AND-DRIFT.md](TOOL-DISCOVERY-AND-DRIFT.md) §1, §4 | `mcp_gateway_registry_test.go` (PR-2) | Yes — `catalog_revision` bumps on change; capped `maxSnapMCPServers` (proposed) | Std — a server entry removed from a newer CP is treated by an older DP as "unchanged" only if `catalog_revision` didn't bump; DP MUST reject serving an unknown server ID rather than guess | Metadata-only (endpoint + fingerprint; no server-side credential) |
| `mcp_gateway_server_approval_mode` | Gateway | Server registry entries | `mcp.gateway.server_approval_mode` | `CULVERT_MCP_GW_SERVER_APPROVAL_MODE` | `-mcp-gateway-server-approval-mode` | `GET/PUT /api/mcp/gateway/settings` | MCP → Gateway → Servers | Yes | enum `manual` (default) \| `auto-on-fingerprint-match`; `auto` MUST still require an out-of-band fingerprint registration, never TOFU-on-first-connect | [TOOL-DISCOVERY-AND-DRIFT.md](TOOL-DISCOVERY-AND-DRIFT.md) §4, MCP-T-020 | `mcp_gateway_server_approval_test.go` (PR-2) | Yes — meta enum | Override — MUST default to `manual`; an unrecognized/downgraded value falls back to `manual`, never `auto` | None |
| `mcp_gateway_tool_catalog_snapshot` | Gateway | Tool catalog/approval | (derived — not directly operator-editable; captured from live discovery) `mcp.gateway.catalog_snapshot_id` | n/a | n/a | `GET /api/mcp/gateway/catalog` (viewer read); mutation only via approval-workflow endpoints below | MCP → Gateway → Tool Catalog | Yes | each tool entry carries a canonical fingerprint (schema + description hash) per [TOOL-DISCOVERY-AND-DRIFT.md](TOOL-DISCOVERY-AND-DRIFT.md) §1 | [TOOL-DISCOVERY-AND-DRIFT.md](TOOL-DISCOVERY-AND-DRIFT.md) §1–§2 | `mcp_gateway_catalog_fingerprint_test.go` (PR-2) | Yes — `catalog_revision` (see [CP-DP-HA-MODEL.md](CP-DP-HA-MODEL.md) §2); capped `maxSnapMCPTools` (proposed) | Std | None |
| `mcp_gateway_tool_approval_state` | Gateway | Tool catalog/approval | `mcp.gateway.tools[].approval_state` (list, per tool: pending/approved/revoked) | n/a (list) | n/a (list) | `POST /api/mcp/gateway/tools/{fingerprint}/approve`, `.../revoke` (admin-only write, viewer read) | MCP → Gateway → Tool Catalog (approval queue) | Yes | state transitions MUST be `pending → approved` or `any → revoked`; a schema/description drift on an already-approved tool forces it back to `pending` (never silently re-approved) | [TOOL-DISCOVERY-AND-DRIFT.md](TOOL-DISCOVERY-AND-DRIFT.md) §2 ("HARD RULE — never automatic allow") | `mcp_gateway_tool_approval_test.go` (PR-2) | Yes — part of `catalog_revision` | Override — a drifted fingerprint MUST reset to `pending` on every node, including one that was already synced `approved` — this is a narrowing, so it is safe to force-apply even mid-rollout | None |
| `mcp_gateway_unknown_tool_default_action` | Gateway | Tool catalog/approval | `mcp.gateway.unknown_tool_default_action` | `CULVERT_MCP_GW_UNKNOWN_TOOL_DEFAULT_ACTION` | `-mcp-gateway-unknown-tool-default-action` | `GET/PUT /api/mcp/gateway/settings` (admin-only write) | MCP → Gateway → Policy (locked control, admin-only) | Yes | enum restricted to `{DENY, QUARANTINE, REQUIRE_APPROVAL}` from the [nine policy actions](#nine-policy-actions-reference) — **`ALLOW`/`ALLOW_ONCE`/`ALLOW_FOR_SESSION`/`ALLOW_WITH_REDACTION`/`MONITOR` are rejected at validation** | [SECURITY-REQUIREMENTS.md](SECURITY-REQUIREMENTS.md) MCP-TOOL (MCP-T-017 unknown-tool auto-allow, Critical) | `mcp_gateway_unknown_tool_test.go` (PR-6) | Yes — meta enum | Override — the validation restriction itself MUST NOT be removable by config; even a downgraded node keeps a hardcoded `DENY` if this field is absent/corrupt | None |
| `mcp_gateway_policy_bundle_ref` | Gateway | Policy bundle | `mcp.gateway.policy_bundle_id` | `CULVERT_MCP_GW_POLICY_BUNDLE_ID` | `-mcp-gateway-policy-bundle-id` | `GET/PUT /api/mcp/gateway/policy`, `POST /api/mcp/gateway/policy/bundles` (admin write, viewer read) | MCP → Gateway → Policy | Yes | references an existing uploaded/validated bundle ID; a bundle referencing an undefined reason-code prefix fails validation | [SECURITY-REQUIREMENTS.md](SECURITY-REQUIREMENTS.md) MCP-POLICY-001..007 | `mcp_gateway_policy_bundle_test.go` (PR-6) | Yes — `policy_revision` (already on `ConfigSnapshot` today, per [CP-DP-HA-MODEL.md](CP-DP-HA-MODEL.md) §2) | Std | None |
| `mcp_gateway_policy_default_action` | Gateway | Policy bundle | `mcp.gateway.policy_default_action` | `CULVERT_MCP_GW_POLICY_DEFAULT_ACTION` | `-mcp-gateway-policy-default-action` | `GET/PUT /api/mcp/gateway/policy` (admin-only write) | MCP → Gateway → Policy (locked control) | Yes | enum from the nine policy actions; **default `DENY`** — mirrors the existing SWG default-deny posture (`policy.go:1142`, `proxy.go:543-556`) | [SECURITY-REQUIREMENTS.md](SECURITY-REQUIREMENTS.md) MCP-POLICY-001 (default deny) | `mcp_gateway_default_deny_test.go` (PR-6) | Yes — meta enum | Override — MUST default to `DENY`; a corrupt/absent value never resolves to any `ALLOW*` variant | None |
| `mcp_gateway_reason_code_catalog_version` | Gateway | Policy bundle | `mcp.gateway.reason_code_catalog_version` | `CULVERT_MCP_GW_REASON_CODE_CATALOG_VERSION` | `-mcp-gateway-reason-code-catalog-version` | `GET /api/mcp/gateway/policy` (read-only; version is CI-pinned, not operator-editable) | MCP → Gateway → Policy (read-only display) | Yes | monotonic int; a policy bundle referencing reason codes from a newer catalog version than the running binary fails validation | [MCP-POLICY-MODEL.md](MCP-POLICY-MODEL.md) Appendix C (reason-code prefixes: `MCP.AUTH`, `MCP.SERVER`, `MCP.TOOL`, `MCP.POLICY`, `MCP.CREDENTIAL`, `MCP.INSPECTION`, `MCP.RATE`, `MCP.SYSTEM`, `MCP.MANAGEMENT`) | `mcp_reason_code_catalog_test.go` (PR-6) | Yes — meta int | Std | None |
| `mcp_gateway_credential_profiles` | Gateway | Credential-profile metadata | `mcp.gateway.credential_profiles` (list: profile_id, environment, server_id, tool_class, resource_scope, provider_ref) | n/a (list) | n/a (list) | `GET/POST/PUT/DELETE /api/mcp/gateway/credentials/profiles[/{id}]` (admin-only write, viewer read of metadata only) | MCP → Gateway → Credentials (profile list; **no secret-value field anywhere in this panel**) | Yes | `provider_ref` MUST resolve to a configured provider (Vault/KMS/Secrets-Manager/workload-identity) at validation time; profile MUST NOT span more privilege than its narrowest dimension ([AUTH-AND-CREDENTIAL-MODEL.md](AUTH-AND-CREDENTIAL-MODEL.md) §7.1) | [AUTH-AND-CREDENTIAL-MODEL.md](AUTH-AND-CREDENTIAL-MODEL.md) §7.1, MCP-CRED-002 | `mcp_gateway_credential_profile_test.go` (PR-4) | Yes — `credential_revision` (metadata only, net-new per [CP-DP-HA-MODEL.md](CP-DP-HA-MODEL.md) §2); capped `maxSnapCredentialProfiles` (proposed) | Std | **Metadata-only** — profile ID + environment/server/tool-class/resource-scope + provider reference; the resolved secret is fetched at call time from the provider and never written to any config surface (MCP-CRED-001, MCP-CRED-004) |
| `mcp_gateway_credential_broker_fail_mode` | Gateway | Credential-profile metadata | `mcp.gateway.credential_broker_fail_mode` | `CULVERT_MCP_GW_CRED_BROKER_FAIL_MODE` | `-mcp-gateway-credential-broker-fail-mode` | `GET/PUT /api/mcp/gateway/credentials/settings` (admin-only write) | MCP → Gateway → Credentials → Broker | Yes | enum `fail-closed` (default, required for write/high-risk per the broker failure matrix) \| `fail-open-if-cached-and-policy-permits` (read-only/low-risk only — rejected at validation if applied to a write/high-risk profile) | [AUTH-AND-CREDENTIAL-MODEL.md](AUTH-AND-CREDENTIAL-MODEL.md) §7.3, MCP-CRED-006 | `mcp_gateway_broker_failure_test.go` (PR-4) | No — node-local runtime posture, not synced (a stale broker-mode sync during a partition would itself be a failure-semantics hazard) | Override — MUST default `fail-closed`; write/high-risk profiles can never resolve to fail-open regardless of stored value (hardcoded floor, not a config override) | None |
| `mcp_gateway_credential_cache_ttl` | Gateway | Credential-profile metadata | `mcp.gateway.credential_cache_ttl` | `CULVERT_MCP_GW_CREDENTIAL_CACHE_TTL` | `-mcp-gateway-credential-cache-ttl` | `GET/PUT /api/mcp/gateway/credentials/settings` | MCP → Gateway → Credentials → Broker | Yes | Go duration; bounded max (proposed ceiling, mirrors the `maxEntries`/`ttl` bounds pattern in `internal/autoexclude`) | [AUTH-AND-CREDENTIAL-MODEL.md](AUTH-AND-CREDENTIAL-MODEL.md) §7.3, MCP-CRED-005 (cache MUST be bounded and encrypted at rest) | `mcp_gateway_credential_cache_test.go` (PR-4) | No — cache is node-local runtime state, not config | Std | **Metadata-only** — TTL is a config scalar; the cache it governs holds provider-issued short-lived credentials at rest under `internal/secret` KEK containment, never in this config surface |
| `mcp_gateway_inspect_max_payload_bytes` | Gateway | Inspection limits | `mcp.gateway.inspect.max_payload_bytes` | `CULVERT_MCP_GW_INSPECT_MAX_PAYLOAD_BYTES` | `-mcp-gateway-inspect-max-payload-bytes` | `GET/PUT /api/mcp/gateway/inspection/settings` (admin write, viewer read) | MCP → Gateway → Inspection | Yes | positive int; ceiling constant | [SECURITY-REQUIREMENTS.md](SECURITY-REQUIREMENTS.md) MCP-INSP-001 | `mcp_gateway_inspect_limits_test.go` (PR-7) | Yes — meta int | Std | None |
| `mcp_gateway_inspect_max_schema_depth` | Gateway | Inspection limits | `mcp.gateway.inspect.max_schema_depth` | `CULVERT_MCP_GW_INSPECT_MAX_SCHEMA_DEPTH` | `-mcp-gateway-inspect-max-schema-depth` | `GET/PUT /api/mcp/gateway/inspection/settings` | MCP → Gateway → Inspection | Yes | positive int; ceiling constant | [SECURITY-REQUIREMENTS.md](SECURITY-REQUIREMENTS.md) MCP-INSP-001 | `mcp_gateway_inspect_limits_test.go` (PR-7) | Yes — meta int | Std | None |
| `mcp_gateway_inspect_max_field_count` | Gateway | Inspection limits | `mcp.gateway.inspect.max_field_count` | `CULVERT_MCP_GW_INSPECT_MAX_FIELD_COUNT` | `-mcp-gateway-inspect-max-field-count` | `GET/PUT /api/mcp/gateway/inspection/settings` | MCP → Gateway → Inspection | Yes | positive int; ceiling constant | [SECURITY-REQUIREMENTS.md](SECURITY-REQUIREMENTS.md) MCP-INSP-001 | `mcp_gateway_inspect_limits_test.go` (PR-7) | Yes — meta int | Std | None |
| `mcp_gateway_inspect_output_max_bytes` | Gateway | Inspection limits | `mcp.gateway.inspect.output_max_bytes` | `CULVERT_MCP_GW_INSPECT_OUTPUT_MAX_BYTES` | `-mcp-gateway-inspect-output-max-bytes` | `GET/PUT /api/mcp/gateway/inspection/settings` | MCP → Gateway → Inspection | Yes | positive int; paired truncation policy (`truncate` \| `reject`) | [SECURITY-REQUIREMENTS.md](SECURITY-REQUIREMENTS.md) MCP-INSP-002 | `mcp_gateway_output_bound_test.go` (PR-7) | Yes — meta int + enum | Std | None |
| `mcp_gateway_inspect_redaction_profile` | Gateway | Inspection limits | `mcp.gateway.inspect.redaction_profile` | `CULVERT_MCP_GW_INSPECT_REDACTION_PROFILE` | `-mcp-gateway-inspect-redaction-profile` | `GET/PUT /api/mcp/gateway/inspection/settings` | MCP → Gateway → Inspection | Yes | references an `internal/redaction` `DataClass` profile ID | [SECURITY-REQUIREMENTS.md](SECURITY-REQUIREMENTS.md) MCP-INSP-003 | `mcp_gateway_redaction_test.go` (PR-7) | Yes — meta string ref | Std | None |
| `mcp_gateway_inspect_ssrf_policy_ref` | Gateway | Inspection limits | `mcp.gateway.inspect.ssrf_policy_ref` | `CULVERT_MCP_GW_INSPECT_SSRF_POLICY_REF` | `-mcp-gateway-inspect-ssrf-policy-ref` | `GET/PUT /api/mcp/gateway/inspection/settings` | MCP → Gateway → Inspection | Yes | references the shared private-IP/metadata deny policy (reuses `internal/ssrf` `PrivateIP`/`PrivateHost`, not a reimplementation — repo note: redirect guards are currently re-implemented per-client with no shared helper; MCP-INSP-006 recommends a shared guard) | [SECURITY-REQUIREMENTS.md](SECURITY-REQUIREMENTS.md) MCP-INSP-004 | `mcp_gateway_ssrf_test.go` (PR-7) | Yes — meta string ref | Std | None |
| `mcp_gateway_inspect_dns_pinning_enabled` | Gateway | Inspection limits | `mcp.gateway.inspect.dns_pinning_enabled` | `CULVERT_MCP_GW_INSPECT_DNS_PINNING_ENABLED` | `-mcp-gateway-inspect-dns-pinning-enabled` | `GET/PUT /api/mcp/gateway/inspection/settings` (admin-only write) | MCP → Gateway → Inspection (locked control) | Yes | bool; **default `true`**, disabling requires an explicit risk-acknowledgement flag | [SECURITY-REQUIREMENTS.md](SECURITY-REQUIREMENTS.md) MCP-INSP-005 (resolve→connect DNS pinning, rebinding TOCTOU) | `mcp_gateway_dns_pinning_test.go` (PR-7) | Yes — meta bool | Override — MUST default `true`; unknown/corrupt value resolves to `true`, never `false` | None |
| `mcp_gateway_inspect_redirect_max_hops` | Gateway | Inspection limits | `mcp.gateway.inspect.redirect_max_hops` | `CULVERT_MCP_GW_INSPECT_REDIRECT_MAX_HOPS` | `-mcp-gateway-inspect-redirect-max-hops` | `GET/PUT /api/mcp/gateway/inspection/settings` | MCP → Gateway → Inspection | Yes | small positive int (proposed ceiling ≤10); each hop re-checked against SSRF policy | [SECURITY-REQUIREMENTS.md](SECURITY-REQUIREMENTS.md) MCP-INSP-006 | `mcp_gateway_redirect_test.go` (PR-7) | Yes — meta int | Std | None |
| `mcp_gateway_inspect_injection_labeling_enabled` | Gateway | Inspection limits | `mcp.gateway.inspect.injection_labeling_enabled` | `CULVERT_MCP_GW_INSPECT_INJECTION_LABELING_ENABLED` | `-mcp-gateway-inspect-injection-labeling-enabled` | `GET/PUT /api/mcp/gateway/inspection/settings` | MCP → Gateway → Inspection | Yes | bool; labeling is advisory (best-effort), never a silent-trust bypass when disabled | [SECURITY-REQUIREMENTS.md](SECURITY-REQUIREMENTS.md) MCP-INSP-007 | `mcp_gateway_injection_label_test.go` (PR-7) | Yes — meta bool | Std | None |
| `mcp_gateway_event_spool_path` | Gateway | Event pipeline | `mcp.gateway.events.spool_path` | `CULVERT_MCP_GW_EVENT_SPOOL_PATH` | `-mcp-gateway-event-spool-path` | `GET/PUT /api/mcp/gateway/events/settings` (admin-only write) | MCP → Gateway → Events | Yes | writable directory; MUST NOT alias `/data/config_versions` or other existing durable stores | [EVENT-MODEL.md](EVENT-MODEL.md) §4 ("do not reuse the debug ring") | `mcp_gateway_event_spool_test.go` (PR-8) | No — node-local durability path, not a config value to sync | Std | None |
| `mcp_gateway_event_export_target` | Gateway | Event pipeline | `mcp.gateway.events.export_target` (sink config: syslog/OTLP/file ref) | `CULVERT_MCP_GW_EVENT_EXPORT_TARGET` | `-mcp-gateway-event-export-target` | `GET/PUT /api/mcp/gateway/events/settings` | MCP → Gateway → Events | Yes | reuses existing sink config shapes (`internal/syslog`, `otlp.go`) rather than a fourth bespoke sink type | [EVENT-MODEL.md](EVENT-MODEL.md) §4, §6 | `mcp_gateway_event_export_test.go` (PR-8) | No | Std | None |
| `mcp_gateway_event_loss_policy` | Gateway | Event pipeline | `mcp.gateway.events.loss_policy` | `CULVERT_MCP_GW_EVENT_LOSS_POLICY` | `-mcp-gateway-event-loss-policy` | `GET/PUT /api/mcp/gateway/events/settings` (admin-only write) | MCP → Gateway → Events (locked control) | Yes | enum `fail-closed` (deny the triggering call rather than lose its event) \| `degrade-and-alert` (queue saturation ⇒ defined degraded mode + alert, never silent drop) — **silent drop is not a selectable value** | [SECURITY-REQUIREMENTS.md](SECURITY-REQUIREMENTS.md) MCP-EVENT-001/002 | `mcp_gateway_event_loss_test.go` (PR-8) | No | Override — silent-drop-on-full is structurally excluded from the enum, not merely defaulted away | None |
| `mcp_gateway_event_redaction_profile` | Gateway | Event pipeline | `mcp.gateway.events.redaction_profile` | `CULVERT_MCP_GW_EVENT_REDACTION_PROFILE` | `-mcp-gateway-event-redaction-profile` | `GET/PUT /api/mcp/gateway/events/settings` | MCP → Gateway → Events | Yes | references an `internal/redaction` `DataClass` profile; MUST NOT permit a profile that stores raw tokens/secrets/complete raw args or outputs (structurally enforced, not operator-relaxable) | [SECURITY-REQUIREMENTS.md](SECURITY-REQUIREMENTS.md) MCP-EVENT-003 | `mcp_gateway_event_redaction_test.go` (PR-8) | No | Std | None |
| `mcp_gateway_event_replay_id_enabled` | Gateway | Event pipeline | `mcp.gateway.events.replay_id_enabled` | `CULVERT_MCP_GW_EVENT_REPLAY_ID_ENABLED` | `-mcp-gateway-event-replay-id-enabled` | `GET /api/mcp/gateway/events/settings` (read-only — see Note) | MCP → Gateway → Events (read-only display) | Yes | bool; proposed **read-only `true`** — MCP-EVENT-004 requires replay/correlation IDs unconditionally, so this exists as a documented invariant, not an operator-disable switch | [SECURITY-REQUIREMENTS.md](SECURITY-REQUIREMENTS.md) MCP-EVENT-004 | `mcp_gateway_event_replay_id_test.go` (PR-8) | No | Override — cannot be disabled; field is exposed for observability/documentation, not mutation | None |
| `mcp_gateway_rate_limit_rps` / `_burst` / `_per_principal` | Gateway | Availability | `mcp.gateway.rate_limit.{rps,burst,per_principal}` | `CULVERT_MCP_GW_RATE_LIMIT_RPS` / `_BURST` / `_PER_PRINCIPAL` | `-mcp-gateway-rate-limit-rps` / `-...-burst` / `-...-per-principal` | `GET/PUT /api/mcp/gateway/settings` | MCP → Gateway → Limits | Yes | positive ints; `burst ≥ rps`; sharded-limiter pattern (mirrors the existing 64-shard SWG rate limiter) | [SECURITY-REQUIREMENTS.md](SECURITY-REQUIREMENTS.md) MCP-OPS-002 | `mcp_gateway_ratelimit_test.go` (PR-5) | No | Std | None |
| `mcp_gateway_max_sse_clients` | Gateway | Availability | `mcp.gateway.max_sse_clients` | `CULVERT_MCP_GW_MAX_SSE_CLIENTS` | `-mcp-gateway-max-sse-clients` | `GET/PUT /api/mcp/gateway/settings` | MCP → Gateway → Limits | Yes | positive int; evict-on-slow beyond cap (mirrors `internal/sse` `DefaultMaxClients=256`) | [SECURITY-REQUIREMENTS.md](SECURITY-REQUIREMENTS.md) MCP-OPS-002 (SSE exhaustion, MCP-T-042) | `mcp_gateway_sse_bound_test.go` (PR-5) | No | Std | None |
| `mcp_gateway_max_concurrent_sessions` | Gateway | Availability | `mcp.gateway.max_concurrent_sessions` | `CULVERT_MCP_GW_MAX_CONCURRENT_SESSIONS` | `-mcp-gateway-max-concurrent-sessions` | `GET/PUT /api/mcp/gateway/settings` | MCP → Gateway → Limits | Yes | positive int | [SECURITY-REQUIREMENTS.md](SECURITY-REQUIREMENTS.md) MCP-OPS-002 | `mcp_gateway_session_bound_test.go` (PR-5) | No | Std | None |
| `mcp_gateway_snapshot_sync_enabled` | Gateway | CP/DP snapshot toggle | `mcp.gateway.cp_dp_sync_enabled` | `CULVERT_MCP_GW_CPDP_SYNC_ENABLED` | `-mcp-gateway-cp-dp-sync-enabled` | `GET/PUT /api/mcp/gateway/settings` (admin-only write) | MCP → Gateway → General (advanced) | Yes | bool; **default `false`** until PR-10 ships the extended `ConfigSnapshot` fields below | [CP-DP-HA-MODEL.md](CP-DP-HA-MODEL.md) §1, §3 | `mcp_gateway_cpdp_test.go` (PR-10) | Yes — meta bool gating whether the fields below are populated | Std | None |
| `mcp_gateway_snapshot_catalog_revision` | Gateway | CP/DP snapshot toggle | (derived; not directly operator-set) `catalog_revision` on `ConfigSnapshot` | n/a | n/a | `GET /api/mcp/gateway/catalog` (surfaced read-only) | MCP → Gateway → Tool Catalog (revision badge) | Yes | monotonic int; **[FACT]** no analog exists on today's `ConfigSnapshot` (`controlplane_snapshot.go:22-112`) — net-new field, per [CP-DP-HA-MODEL.md](CP-DP-HA-MODEL.md) §2 | [CP-DP-HA-MODEL.md](CP-DP-HA-MODEL.md) §2, MCP-CPDP-001 | `mcp_snapshot_schema_test.go` (PR-10) | Yes — meta int, required field per MCP-CPDP-001 | Std | None |
| `mcp_gateway_snapshot_credential_revision` | Gateway | CP/DP snapshot toggle | (derived) `credential_revision` on `ConfigSnapshot` | n/a | n/a | `GET /api/mcp/gateway/credentials/profiles` (surfaced read-only) | MCP → Gateway → Credentials (revision badge) | Yes | monotonic int; **[FACT]** net-new — repo carries `SessionHMAC` (a different, existing secret-sync mechanism) but no credential-**profile** revision concept ([CP-DP-HA-MODEL.md](CP-DP-HA-MODEL.md) §2) | [CP-DP-HA-MODEL.md](CP-DP-HA-MODEL.md) §2, MCP-CPDP-001 | `mcp_snapshot_schema_test.go` (PR-10) | Yes — meta int, required field | Std | **Metadata-only** — the revision counts changes to profile *metadata*, never carries or implies a secret value |
| `mcp_gateway_snapshot_min_dp_version` | Gateway | CP/DP snapshot toggle | `mcp.gateway.min_dp_version` (CP-set publish gate) | `CULVERT_MCP_GW_MIN_DP_VERSION` | `-mcp-gateway-min-dp-version` | `GET/PUT /api/mcp/gateway/settings` (admin-only write) | MCP → Gateway → General (advanced) | Yes | semver string; DP below this version MUST NOT apply the snapshot (per MCP-CPDP-003) | [CP-DP-HA-MODEL.md](CP-DP-HA-MODEL.md) §2, §6, MCP-CPDP-001/003 | `mcp_mixed_version_test.go` (PR-10) | Yes — meta string, required field | Override — this field's whole purpose is to make a DP refuse a snapshot; there is no "ignore and apply anyway" fallback | None |
| `mcp_gateway_snapshot_content_hash` | Gateway | CP/DP snapshot toggle | (derived; computed, not operator-set) `content_hash` on `ConfigSnapshot` | n/a | n/a | `GET /api/mcp/gateway/settings` (surfaced read-only, diagnostics) | MCP → Gateway → General (diagnostics) | Yes | deterministic digest over snapshot payload; **[FACT]** no content-hash field/computation exists on today's `ConfigSnapshot` ([CP-DP-HA-MODEL.md](CP-DP-HA-MODEL.md) §2) | [CP-DP-HA-MODEL.md](CP-DP-HA-MODEL.md) §2, §7 (rollback identity), MCP-CPDP-001/002 | `mcp_snapshot_integrity_test.go` (PR-10) | Yes — meta string, required field; a corrupt/truncated snapshot MUST be whole-rejected, never partially applied | Std | None |
| `mcp_gateway_snapshot_signature` | Gateway | CP/DP snapshot toggle | (derived; computed, not operator-set) `signature` on `ConfigSnapshot` | n/a | n/a | `GET /api/mcp/gateway/settings` (surfaced read-only, diagnostics) | MCP → Gateway → General (diagnostics) | Yes | signing-scheme selection deferred to [OPEN-DECISIONS.md](OPEN-DECISIONS.md); **[FACT]** no snapshot signing exists today — SWG CP↔DP integrity is mTLS+epoch only; ed25519 signing exists only in the unrelated release-catalog subsystem, cited as reusable prior art, not an existing MCP capability | [CP-DP-HA-MODEL.md](CP-DP-HA-MODEL.md) §2, MCP-CPDP-001/002 | `mcp_snapshot_signature_test.go` (PR-10) | Yes — meta string, required field | Std | None |
| `mcp_gateway_connector_mode` | Gateway | Connector/DMZ config | `mcp.gateway.connector_mode` | `CULVERT_MCP_GW_CONNECTOR_MODE` | `-mcp-gateway-connector-mode` | `GET/PUT /api/mcp/gateway/connector/settings` (admin-only write) | MCP → Gateway → Connectivity | Yes | enum `local-client` (Model A, default) \| `outbound-connector` (Model B) \| `dmz-endpoint` (Model C, requires `mcp_gateway_dmz_waf_profile_ref` + explicit risk-acceptance flag) | [ON-PREM-CONNECTIVITY.md](ON-PREM-CONNECTIVITY.md) §2–§4 | `mcp_gateway_connector_mode_test.go` (post-V1 connector slice — D-8; `dmz-endpoint` default-off/deferred — D-9) | Yes — meta enum | Std | **V1: only `local-client` is supported** (D-8); `outbound-connector` is a post-V1 slice, `dmz-endpoint` is default-off/deferred (D-9). |
| `mcp_gateway_connector_mtls_profile_ref` | Gateway | Connector/DMZ config | `mcp.gateway.connector_mtls_profile_ref` | `CULVERT_MCP_GW_CONNECTOR_MTLS_PROFILE_REF` | `-mcp-gateway-connector-mtls-profile-ref` | `GET/PUT /api/mcp/gateway/connector/settings` | MCP → Gateway → Connectivity | Yes | references a cert/key-pair provisioning profile (paths/rotation config); required when `connector_mode = outbound-connector` | [ON-PREM-CONNECTIVITY.md](ON-PREM-CONNECTIVITY.md) §3, MCP-CONNECT-001 | `mcp_gateway_connector_mtls_test.go` (post-V1 connector slice — D-8) | No — node-local connector identity, not CP-published | Std | **Metadata-only** — a reference to where the private key material lives (filesystem path / provider), never the key itself |
| `mcp_gateway_connector_cert_rotation_interval` | Gateway | Connector/DMZ config | `mcp.gateway.connector_cert_rotation_interval` | `CULVERT_MCP_GW_CONNECTOR_CERT_ROTATION_INTERVAL` | `-mcp-gateway-connector-cert-rotation-interval` | `GET/PUT /api/mcp/gateway/connector/settings` | MCP → Gateway → Connectivity | Yes | Go duration; rotation MUST NOT require an outage (MCP-CONNECT-002) | [ON-PREM-CONNECTIVITY.md](ON-PREM-CONNECTIVITY.md) §3, MCP-CONNECT-002 | `mcp_gateway_connector_rotation_test.go` (post-V1 connector slice — D-8) | No | Std | None |
| `mcp_gateway_connector_reconnect_backoff_policy` | Gateway | Connector/DMZ config | `mcp.gateway.connector_reconnect_backoff` (base/max/ceiling) | `CULVERT_MCP_GW_CONNECTOR_RECONNECT_BACKOFF` | `-mcp-gateway-connector-reconnect-backoff` | `GET/PUT /api/mcp/gateway/connector/settings` | MCP → Gateway → Connectivity | Yes | bounded backoff (base/max/retry-ceiling); an unbounded reconnect loop is itself an availability risk (MCP-CONNECT-002) | [ON-PREM-CONNECTIVITY.md](ON-PREM-CONNECTIVITY.md) §3, MCP-CONNECT-002 | `mcp_gateway_connector_backoff_test.go` (post-V1 connector slice — D-8) | No | Std | None |
| `mcp_gateway_dmz_waf_profile_ref` | Gateway | Connector/DMZ config | `mcp.gateway.dmz_waf_profile_ref` | `CULVERT_MCP_GW_DMZ_WAF_PROFILE_REF` | `-mcp-gateway-dmz-waf-profile-ref` | `GET/PUT /api/mcp/gateway/connector/settings` (admin-only write) | MCP → Gateway → Connectivity (Model C section, requires signed-off risk acceptance before enable) | Yes | required, non-empty when `connector_mode = dmz-endpoint`; validation rejects enabling Model C without this AND `mcp_gateway_origin_host_allowlist` set | [ON-PREM-CONNECTIVITY.md](ON-PREM-CONNECTIVITY.md) §4, MCP-CONNECT-003 | `mcp_gateway_dmz_test.go` (post-V1; **DMZ default-off/deferred** — D-9) | No | Std | **D-9: DMZ (Model C) not supported in V1, disabled by default; requires separate architecture + signed risk acceptance if ever offered.** |
| `mcp_gateway_tenant_binding_mode` | Gateway | Per-tenant settings | `mcp.gateway.tenant_binding_mode` | `CULVERT_MCP_GW_TENANT_BINDING_MODE` | `-mcp-gateway-tenant-binding-mode` | `GET/PUT /api/mcp/gateway/settings` (admin-only write) | MCP → Gateway → Tenancy | Yes | enum `strict` (default — every session, including LAN/VPN-local, MUST be tenant-bound; "internal network" is not itself a tenant boundary) | [SECURITY-REQUIREMENTS.md](SECURITY-REQUIREMENTS.md) MCP-CONNECT-004, MCP-T-010 | `mcp_gateway_tenant_binding_test.go` (PR-3/PR-11) | Yes — meta enum | Override — MUST default and remain `strict`; no config path relaxes tenant binding | None |
| `mcp_gateway_per_tenant_overrides` | Gateway | Per-tenant settings | `mcp.gateway.tenants[]` (per-tenant: rate-limit override, policy-bundle-ref override) | n/a (list) | n/a (list) | `GET/PUT /api/mcp/gateway/tenants/{tenant}/overrides` (admin write, viewer read) | MCP → Gateway → Tenancy (per-tenant table) | Yes | override values MUST be strictly narrower than the global defaults for rate limits (can only tighten, never loosen) and MUST reference an existing policy bundle for policy overrides | [SECURITY-REQUIREMENTS.md](SECURITY-REQUIREMENTS.md) MCP-CONNECT-004, MCP-PRIVACY-001..003 | `mcp_gateway_tenant_override_test.go` (PR-9/PR-11) | Yes — capped `maxSnapMCPTenantOverrides` (proposed) | Std | None |

### Nine policy actions (reference)

`ALLOW`, `DENY`, `MONITOR`, `QUARANTINE`, `REQUIRE_CONFIRMATION`, `REQUIRE_APPROVAL`, `ALLOW_ONCE`,
`ALLOW_FOR_SESSION`, `ALLOW_WITH_REDACTION`. Every policy-action-typed field in the matrix above draws
from this fixed enum; no field introduces a tenth action.

---

## Cross-references

- [`SECURITY-REQUIREMENTS.md`](SECURITY-REQUIREMENTS.md) — the canonical `MCP-*` requirement IDs cited
  throughout this matrix (MCP-AUTH, MCP-ID, MCP-SERVER, MCP-TOOL, MCP-POLICY, MCP-CRED, MCP-INSP,
  MCP-EVENT, MCP-CPDP, MCP-HA, MCP-CONNECT, MCP-MGMT, MCP-PRIVACY, MCP-SUPPLY, MCP-OPS).
- [`CP-DP-HA-MODEL.md`](CP-DP-HA-MODEL.md) — the authoritative spec for the five proposed
  `ConfigSnapshot` fields this matrix's snapshot rows surface as config (`catalog_revision`,
  `credential_revision`, `minimum_dp_version`, `content_hash`, `signature`), and for fencing/rollback/
  mixed-version behavior.
- [`THREAT-MODEL.md`](THREAT-MODEL.md) — the canonical `MCP-T-*` threat IDs this matrix's validation
  and backward-compatibility overrides are written to close or narrow.
- [`AUTH-AND-CREDENTIAL-MODEL.md`](AUTH-AND-CREDENTIAL-MODEL.md) — credential-profile partitioning
  (environment/server/tool-class/resource-scope) and the broker failure matrix this matrix's
  `mcp_gateway_credential_*` rows implement as config.
- [`TOOL-DISCOVERY-AND-DRIFT.md`](TOOL-DISCOVERY-AND-DRIFT.md) — tool fingerprinting, drift
  classification, and the "never automatic allow" hard rule behind `mcp_gateway_unknown_tool_default_action`
  and `mcp_gateway_tool_approval_state`.
- [`EVENT-MODEL.md`](EVENT-MODEL.md) — durable decision-event categories, redaction, and the explicit
  contrast with the existing (non-durable) audit ring that motivates a dedicated event-pipeline config
  surface distinct from `auditEvent`/`auditEventDiff`.
- [`ON-PREM-CONNECTIVITY.md`](ON-PREM-CONNECTIVITY.md) — Models A/B/C behind
  `mcp_gateway_connector_mode` and its dependent fields.
- [`PROTOCOL-COMPATIBILITY.md`](PROTOCOL-COMPATIBILITY.md) — version negotiation and downgrade policy
  behind the two `*_protocol_version_policy` rows.
- [`RECOMMENDED-ARCHITECTURE.md`](RECOMMENDED-ARCHITECTURE.md) — the shared-platform-vs-separate-
  runtimes doctrine this matrix's Mgmt/Gateway separation implements at the config-surface level.
- [`OPEN-DECISIONS.md`](OPEN-DECISIONS.md) — the deferred choice of extending `config_surfaces.go`
  itself vs. introducing a parallel MCP-specific registry, and the deferred snapshot signing-scheme
  selection.
- [`docs/adr/0024-mcp-agent-security-gateway-trust-boundary.md`](../../adr/0024-mcp-agent-security-gateway-trust-boundary.md)
  — the numbered ADR (promoted from the in-package proposal on 2026-07-24; `Status: Proposed`, ratification
  pending) recording the trust-boundary decisions and the D-2/D-5/D-8/D-9/D-13 closures. This matrix's
  config-surface plan is downstream of that decision — notably: `mcp_gateway_connector_*` fields are a
  **post-V1 connector slice** (D-8) and `mcp_gateway_dmz_*` fields are **default-off/deferred** (D-9).

## Risk note

Low for the read-only Phase 1 investigation, but the current repository test baseline remains
unverified in this session.
