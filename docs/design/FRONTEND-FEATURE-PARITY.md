# Frontend Feature Parity Matrix

- **Status**: Baseline (2026-08-21). This is the parity gate for the frontend replacement: FE-8
  cutover requires every row DONE or explicitly descoped with sign-off. Measured from
  `static/index.html` (21,565 lines, 38 views), `ui_routes_meta.go` (229 routes / 343 method
  rows at baseline; the pin is **243** routes since 2F-C — `ui_routes_meta_test.go` /
  `d0_helpers_test.go` — and the generated per-method inventory is
  `api/route-classification.yaml`; the "§7 accounting table" this line used to point at no
  longer exists in `FRONTEND-SECURITY-CONTRACT.md`), and the
  discovery inventory in `FRONTEND-CURRENT-STATE.md`.
- Legend — **Role**: minimum role that sees the panel (`data-min-role` and/or imperative guard;
  backend per-endpoint roles remain authoritative). **Mut**: panel contains mutating calls.
  **Destr**: contains destructive/hard-to-reverse operations (T3 = typed-word tier-3 ceremony,
  2P = two-phase server token, MCP-D = MCP danger dialog). **Audit**: backend marks its
  mutations `AuditExpected`. **Poll/SSE**: live-data dependencies. **Tests**: M = markup-pinned
  Go scan, G = `*_gui_test.go` shell scan, E = playwright-go `uie2e` spec, A = API-level Go
  tests (kept regardless). **Risk**: migration risk (H/M/L).

## A. Views (38)

| ID | View (`data-view`) | Endpoints (families) | Role | Mut | Destr | Audit | Poll/SSE | Tests | Risk |
|---|---|---|---|---|---|---|---|---|---|
| FE-V01 | `dashboard` | `/api/stats`, `/api/timeseries`, `/api/top-hosts`, `/api/country-traffic`, `/api/dashboard/{health,threats,top-rules}`, `/api/health/explain` | viewer | no | — | — | 3 s tick + SSE `/api/events` (counters, countries, LIVE pill) | E (LiveSSEDashboard), A | **H** — SSE lifecycle + tick pause/resume semantics + 2 charts (Chart.js is **conditional on the FE-2 gate** — strict-CSP/no-style-mutation proof, else replaced by an internal SVG/CSS implementation; see ADR-FE-001) + posture strip |
| FE-V02 | `livefeed` (Traffic) | `/api/requests`, `/api/logs{,/retention,/purge}`, `/api/export` | viewer | purge/retention | purge (confirm) | yes | 3 s tick | E (TrafficRuleLink…), A | ~~M~~ **MIGRATED (FE-4 + 2A + 2A-M, Batch 2, 2026-08-22)**: Traffic query console (FE-4, ADR-FE-002), stable rule-ID deep links (2A), and the 2A-M History & Storage surface — persistent-history retention visibility, admin retention mutation (enable/disable/days/GB/disk threshold), T2 purge with unknown-outcome truth, and the recent-memory export with owned Blob/download lifecycle. **Truth correction**: `/api/export` exports the in-memory request ring (`reqlog.MaxRing` = 5000 newest entries via `logGet()`), NOT "all logs"/persistent history — the v2 UI labels it "Export recent memory" with explicit scope disclosure. A persistent-history export does not exist in the backend and is NOT claimed; providing one would be a new backend contract (recorded product gap, not a frontend residue). Legacy 3 s tick retires with the legacy UI. |
| FE-V03 | `audit` | `/api/audit` | viewer | no | — | — | on-view load | E (AuditTrail, AuditLog_Filterable) | L |
| FE-V04 | `decexclusions` | `/api/decryption-exclusions{,/tunables}` | viewer (tunables admin) | yes | evict one / clear all / reset tunables (confirms) | yes | on-view | G (decexcl_tunables), A | L |
| FE-V05 | `dechealth` | `/api/decryption/{health,redaction}` | viewer | no | — | — | on-view | A | L |
| FE-V06 | `policy-tester` | `/api/policy/test` | viewer | no (dry-run) | — | — | — | E (PolicyTester) | ~~L~~ **MIGRATED (2A, Batch 2, 2026-08-22)**: `/app/policies/tester` — explicit Run-test only, per-rule skip-reason trace preserved as a dense table, rulebase running/draft truth rendered from the server field, Stage-1 auth block + host category surfaced, discriminated fail-closed decoder, shared explicit-run AbortController wired to the auth boundary, read-only proof (no version/hit/rule mutation). Legacy panel retires with the legacy UI at FE-8. |
| FE-V07 | `mcp-overview` | `/api/mcp/{overview,health,config}` | viewer | no | — | — | on-view + ticketed refresh | A (ui_mcp tests) | M |
| FE-V08 | `mcp-servers` | `/api/mcp/{servers,tools}` | viewer | no | — | — | on-view | A | M — drawer drill-down w/ frame stack |
| FE-V09 | `mcp-decisions` | `/api/mcp/{decisions,decision-explain}` | viewer | no | — | — | on-view | A | M — evidence drawer, shadow-override rendering |
| FE-V10 | `mcp-policies` | `/api/mcp/{policy,policy-simulate,publications,publication-decision}` | viewer (publish operator+) | yes | publish/discard (native confirm today) | yes | — | A | **H** — candidate draft in memory, validate/simulate/compare, never-persist guarantee |
| FE-V11 | `mcp-approvals` | `/api/mcp/{approvals,approval-decision}` | viewer (decide operator+) | yes | four-eyes decisions (MCP-D; `self_approval` surfaced) | yes | ticketed | A | **H** — four-eyes ceremony correctness |
| FE-V12 | `mcp-health` | `/api/mcp/{health,distribution,distribution/acks}` | viewer | no | — | — | on-view | A | M |
| FE-V13 | `mcp-rollout` | `/api/mcp/rollout{,/scope,/scope/validate,/transition,/emergency,/evidence,/rehearse-rollback}`, `/api/mcp/rollback` | viewer (act admin) | yes | kill switch, emergency, transitions, rollback (**MCP-D**: typed phrase, ticket supersede, UNKNOWN-state copy, `production_locked`) | yes | ticketed poll | A (transaction tests) | **H** — the most safety-critical ceremony in the product |
| FE-V14 | `mcp-management` | `/api/mcp/management-access` | viewer | no (read-only by design, D-13) | — | — | on-view | A | L — must stay visibly read-only |
| FE-V15 | `mcp-settings` | `/api/mcp/config` | admin | yes | — | yes | — | A | L |
| FE-V16 | `policy` (Access Rules) | `/api/policy{,/reorder,/move,/draft,/draft/commit,/draft/revert}`, `/api/default-action`, `/api/objects/references` | operator | yes | delete rule, discard staged, default-action change (T3-adjacent), commit | yes | 3 s tick (guarded render) | E ×5 (draft bar, cross-plane, by-ID, reorder), M ×4 | **H** — draft/commit + staged reorder + shadow warnings + version fencing + render ceiling (~200–300 rules). **2A (Batch 2, 2026-08-22): READ surface migrated** — `/app/policies/access-rules` (viewer+, snapshot/refresh per ADR-FE-002, server priority order, Stage-1 excluded with explicit counting, unknown rule types fail closed, draft/running truthfulness callout, `?rule=<ulid>` deep link, 502-rule real-binary scale proof, no polling). **2B (Batch 2, 2026-08-28): WRITE surface migrated** — create/edit/delete by stable ID (full-replacement `AccessRuleWrite` DTO, tri-state wire fidelity, server-owned fields never round-tripped), staged reorder (local permutation, deterministic keyboard controls, fenced apply, 409 = visible discard), draft state (all four modes incl. STRANDED recovery posture), Require Commit ceremony, shared-actor warning, commit review (fresh capture, reviewed-version fence, shadow advisory + tester link, required comment, refreshed-truth-verified success), revert ceremony, structured version-conflict handling, default-action immediate-live T2 ceremony, unknown-outcome doctrine on every mutation class. Every v2 mutation sends `ifVersion`; the 2B.0 backend hardening makes the fence ATOMIC and ordinary writes durable-or-nothing. **Recorded residue: v2 BULK delete deliberately DEFERRED** — the legacy bulk endpoint addresses rules by PRIORITY arrays, which cannot satisfy the stable-ID/fencing doctrine; a stable-ID bulk contract does not exist yet, so the legacy console remains the bulk-delete surface (parity row otherwise complete). `/api/policy/move` stays a backend compatibility path (the v2 client expresses moves through the staged reorder permutation). Legacy 3 s tick retires with the legacy UI. Row done except the recorded bulk residue. |
| FE-V17 | `authpolicy` | `/api/authpolicy{,/reorder}`, `/api/settings/default-auth-outcome` | operator | yes | delete rule; default outcome flip (**T3** OPEN/REQUIRE) | yes | — | M ×4 (authpolicy_phase*), A | **H** — SSO/provider-ref semantics byte-pinned today; simulator stage separation. **2C (Batch 2, 2026-08-28): surface migrated** — `/app/policies/authentication-rules` (viewer+ read; ADMIN-only writes, mirroring the backend's stricter floor — operator is read-only in v2 by design). Stable-ID addressing + always-asserted `ifVersion` against the RUNNING generation (2C.0 backend hardening: atomic `fencedRunningMutate`, durable-or-nothing, stable-ID `{ids}` reorder, strict id contract); dedicated `AuthRuleView`/`AuthRuleWrite` DTOs (unknown outcomes/predicates fail closed → degraded read-only); Exempt rendered as a warning-class waiver with the server note verbatim (never Allow semantics); SSORequired provider refs from the IdP read API with dangling refs marked unresolved and preserved; server warnings verbatim; T2 delete; staged reorder; §9 active-draft warning + the DraftBar `baseStale` critical state; default outcome as TIER-3 typed OPEN/REQUIRE ceremonies with unknown-value fail-closed blocking. Simulator stays on the shared Policy Tester (no second simulator). Legacy `?priority=` addressing remains a backend deprecation-window path only. Row done. |
| FE-V18 | `policylearn` | `/api/policy-learning{,/config,/session,/sessions,/recommendations,/recommendations/generate}` | viewer (ops operator, accept admin) | yes | disable engine; accept-to-draft (armed only) | yes | on-view | M (m5a/m5b wording + role gating), A | M — advisory-only wording is contract; accept/reject fencing (`if_version`). **2C (Batch 2, 2026-08-28): surface migrated** — `/app/policies/learning` (NODE-LOCAL + ADVISORY ONLY from the server's own notes; snapshot model, no polling/automation; factual quality signals only, no invented health/confidence). Governance admin-only (enable/disable T2 with active-session 409 verbatim; guardrail category allowlist; thresholds READ-ONLY — no sliders, M5A contract); session lifecycle operator+ (start/complete T1, cancel T2); generate as an explicit operator action with the engine's factual summary; recommendations decoded full-fidelity with SERVER-computed staleness; **Accept to Policy Draft** preserves the M5B contract exactly (admin, fresh+generated+armed only, body `{id, action, if_version}`, disabled draft rule, §33 post-accept agreement check, deep link to the created rule); reject operator+ decision-only with bounded reason; unconfirmed Accept latches (never blindly repeated). Row done. |
| FE-V19 | `blocklist` | `/api/blocklist{,/mode,/feed,/feed/sync,/exceptions}` | operator | yes | mode flip (**T3** ALLOWLIST/BLOCKLIST), bulk delete, feed remove (double confirm) | yes | tick ≥15 s throttle | E (BlocklistCrossPlane), A | M |
| FE-V20 | `security` (Content & Scanning) | `/api/security`, `/api/security-scan/*` (status, YARA rules/settings/validate/reload, feeds sync, domain-allowlist, exclusions), `/api/dpi`, `/api/content-scan/bypass`, `/api/ssl-bypass` | operator | yes | delete YARA rule file; clear lists | yes | on-view | E (SecurityPanel), A | M — many sub-editors (IP filter, rate limit, YARA editor w/ validate) |
| FE-V21 | `fileblock` | `/api/fileblock{,/profiles}` | operator | yes | clear all extensions; delete profile | yes | tick | E (FileBlockCrossPlane) | L. **2D-C (Batch 2, 2026-08-29): File Profile surface migrated** — `/app/objects/file-profiles` (viewer+ read; operator+ writes). The 2D-C.0A backend promotion makes the stable object ID the authoritative link: `PolicyRule.fileProfileId` is stamped SERVER-side from the submitted name (name = intent; a client-supplied ID is never trusted), enforcement resolves ID-first with the legacy name/compiled-map fallback ONLY for ID-less rules, and a dangling authoritative ID FAILS CLOSED (2D-C final correction: extension-bearing transactions on the rule are blocked — the configured control never silently disappears — with no name retargeting; documented in OBJECT-REFERENCES-BY-ID.md). The v2 read is `GET /api/fileblock/profiles/state` ({profiles, revision}) — one committed snapshot with a content-derived restart-stable revision; every mutation asserts `?ifRevision=` compared inside the store's critical section (2D-C.0B: copy-on-write immutable publication, persist-target-then-swap durable-or-nothing commits, shared structured revision 409). Rename is a TRUE rename (ID preserved; server cascades the display name onto running rules and an active draft candidate; truth callout in the editor); one-extension-per-line editor with a normalization preview (server-authoritative lowercase/leading-dot/dedupe); T2 delete with Where Used preflight and the authoritative referencedBy 409; built-ins (deterministic `builtin-*` IDs) stay fully editable/renamable/deletable — the preserved product behavior, made safe by ID promotion. The Access Rule File Profile selector reads the coherent state endpoint. Legacy global-extension editor (`/api/fileblock`) stays on the legacy console (not part of this row's profile surface migration). Row done. |
| FE-V22 | `cdr` | `/api/cdr/{config,instances,instances/enroll,instances/revoke,policies,health,test}` | operator | yes | revoke instance (confirm ×2 paths); disable | yes | on-view | A | M — enrollment w/ token+fingerprint, test upload |
| FE-V23 | `urlcat` | `/api/urlcat{,/state,/host,/lookup,/feed-status}`, `/api/saas-feed/{settings,status,overrides,refresh}` | operator | yes | delete category | yes | on-view | G (saas_feed) | M — feed status/override interplay. **2D-B (Batch 2, 2026-08-28): surface MIGRATED** — `/app/objects/url-categories` (viewer+ read; operator+ category/host writes; admin SaaS config/overrides/manual refresh). Category NAMES stay the authoritative identity (deliberate architecture — no ULIDs, no rename; `PolicyRule.DestCategory`, group membership and feed resolution all join on the name). The v2 read is `GET /api/urlcat/state` ({categories, revision}) — the legacy raw-array GET is untouched; every mutation asserts `?ifRevision=<ContentFingerprint>` (restart-stable semantic revision, server-owned; ABA-equality accepted by decision) with fence+mutation+durable-publish in one store serialization domain (2D-B.0a: catStore mutMu/saveMu + durable primitives, rollback on persist failure, 10k bound at the store boundary on every path, strict create). Signed SaaS surface: layer separation preserved (admin/baseline vs UT1 'UT1 community feed' labeling vs signed effective view vs overrides); status renders the full nine-state vocabulary with null semantics (never fabricated zeros/epochs) and stale = LKG-serving; settings are official-endpoint-only (`signed_manifest_v1`, no mirror/unsigned), fenced by a content-derived revision inside the AdminSettings save domain (persist-before-apply), managed-DP = CP-owned read-only; overrides are fenced FULL-SET replacement over the durable authority fingerprint with subtree-scope ceremony and counted clear-all; manual refresh renders the verbatim singleflight outcome with one status refetch. Known limitation recorded: UT1 community churn is NOT in the Policy Learning category epoch. Row done. |
| FE-V24 | `catgroups` | `/api/category-groups` | operator | yes | delete group (ref-guarded) | yes | — | E (CategoryGroupEdit_ByID) | L. **2D-A (Batch 2, 2026-08-28): surface migrated** — `/app/objects/category-groups` (viewer+ read; operator+ writes). Stable-ID addressing + always-asserted `ifVersion` against the new durable per-store generation (2D-A.0: `MutateDurable` — fence + mutation + persist + rollback in one critical section; confirmed 2xx = restart-durable; structured 409 conflict contract shared with the policy fence); membership selected from the authoritative URL-category NAME list (categories stay name-authoritative by design — no invented IDs, no category rename here); dangling members badged unresolved and preserved unless explicitly removed; rename by stable ID with the server cascade onto running + draft-candidate denormalized names and the ID-stability truth callout; T1 delete with Where Used preflight (information only) and the server's authoritative referencedBy 409 rendered with stable-ID deep links (the walk now also covers an active draft candidate); unknown-outcome latch; auth-boundary cleanup; `?id=` deep link. Row done. |
| FE-V25 | `decprofiles` | `/api/decryption-profiles` | operator | yes | delete profile (ref-guarded) | yes | — | E (DecryptionProfileEdit_ByID), M (cert-enum lockstep) | M — `dp-cert` option set must stay lockstepped to runtime enum. **2D-A (Batch 2, 2026-08-28): surface migrated** — `/app/objects/decryption-profiles` (viewer+ read; operator+ writes). Same durable/fenced stable-ID mutation contract as FE-V24. Enum lockstep pinned three ways: OpenAPI now declares the closed vocabularies (permissive absent), the frontend constants are compile-time pinned to the generated unions (both directions), and `objects_enum_lockstep_test.go` probes every offered value through `decryptprofile.Validate` forward + reverse (the legacy `dp-cert` GUI lockstep test family is retained unchanged). Security presentation: skip = "certificate verification DISABLED" (never euphemized), onInspectError=fail-open carries the explicit pre-save adaptive-decryption-exclusion warning (distinct from onUnsupported=fail-open — different failure class), InspectHTTP2 tri-state preserved end-to-end (inherit serialized by omission, never collapsed to false), stall bounds pinned to the engine constants, contract-violating profiles render as DEGRADED read-only rows (never coerced). Rename preserves the exclusion scope + SecurityGen (pinned at the engine layer). Row done. |
| FE-V26 | `rewrite` | `/api/rewrite` | operator | yes | delete rule | yes | tick | E (HeaderRewriteCrossPlane) | L. **2D-C (Batch 2, 2026-08-29): surface migrated** — `/app/policies/header-rewrite` (a POLICY surface, deliberately not under Objects). The 2D-C.0C/0D backend promotion adds the durable identity: `stableId` (server-owned UUID, backfilled once at load and persisted through the real AdminSettings owner via the `RewriteRulesSaved` sentinel); the legacy integer `id` remains process-local compatibility metadata — the v2 client never uses it for addressing, deep links, or fencing. The v2 read is `GET /api/rewrite/state` ({rules, revision}) — rules in EVALUATION order (order is semantics; the client never sorts) with a restart-stable content revision over identity + position + host + all header operations; create/delete assert `?ifRevision=` inside the settings serialization domain (fence + mutate + persist in one domain; confirmed 2xx = restart-durable). Create ignores any client-supplied stableId (server mints); delete addresses `?stableId=`. UI: evaluation-order table with per-direction ops summaries and the ordering note, structured editor sections (host scope, request/response set-add-remove), zero-op creates refused, Create + Delete only (no update primitive exists and no reorder is invented — current order is rendered truthfully), stale-fence 409 as the shared structured notice. Bulk doors (import/rollback/CP snapshot) validate stableId uniqueness whole-candidate and preserve restored/synced identities verbatim (interactive/import ID-less entries get fresh identity). Legacy tick retires with the legacy UI. Row done. |
| FE-V27 | `idproviders` | `/api/idp{,/:id,/:id/groups,/discover,/test,/legacy-ldap,/legacy-ldap/import}` | admin | yes | delete IdP | yes | — | E ×2 (LDAP create/edit, viewer-hidden), M (secret redaction) | **H** — write-only secret fields with explicit-clear checkbox semantics (security-pinned) **FE-6A.1 (2026-09-12): READ surface migrated** — `/app/objects/identity-providers` (Objects; viewer floor = uiRoutes `GET /api/idp`; the legacy panel's admin-only nav gate is not carried — v2 minimum read roles come from uiRoutes evidence, the AppShell convention). Read-only by directive: Refresh is the only control (no disabled mutation controls). Renders server facts only: persisted / degraded posture with the bounded reason and "quarantine evidence: recorded" (never the file name), the content-derived document revision, cluster-synced scope, fleet publication result + bounded last-rejection class, operation-ledger posture (retained/unresolved/capacity/audit sink/degraded), per profile identity/type/enabled/entry revision/priority, the write-only-material INDICATOR (`clientSecretConfigured` / `inlineMetadataConfigured` / `bindCredentialConfigured` — never a value), the SSORequired authentication rules that would refuse a delete (`/api/objects/references?type=idp`), the create provenance `operationId`; the node-local legacy YAML LDAP card (present/active/retired/shadowed, directory + bind-credential indicator, bounded `cutoverDurability`, the operation-identified cutover record) and — ADMIN only — the ledger lookup keyed on the enabling profile's create provenance, rendered as its server state (pending / committed + owed audit / aborted + code / outcome_unknown + code; 404 = no retained record; a refusal shows its code). Decoders (`src/api/idp.ts`) refuse any secret-bearing key at any depth and any non-enum server word; manual snapshot, no polling, nothing persisted. Real-binary journeys `frontend/e2e/fe6a1.spec.ts` (AUTH seeded SAML/LDAP + referenced rule, YAMLUP durable cutover, IDPQ quarantined registry). **6A.2 (writes, ceremonies, cutover, repair, test/discover, legacy import) SHIPPED (FE-6A.2, 2026-09-13):** `/app/objects/identity-providers` carries the full admin write surface — create OIDC/SAML/LDAP (document-revision fence + client operationId), update/delete (entry-revision fence; delete = T3 typed provider id; `409 referenced` renders the referencing rules as links), OIDC discovery (https endpoints only), the LDAP directory test (transient credentials, 60 s client deadline, closed step vocabulary, `ok:false` = failed, unverifiable = unproven), legacy YAML LDAP import (T2, "created disabled", credential copied server-side), the legacy-LDAP authority cutover as a typed ceremony on the SERVER-published `cutoverConfirmValue` (backend correction: `?cutoverConfirm=` + `?operationId=` on every cutover-bearing POST/PUT; PUT cutovers are ledger-recorded `idp.update`), registry repair typed on the quarantine evidence (shown only inside the ceremony), and operation recovery (subject-bound non-secret `sessionStorage` marker written before dispatch; Recover via `GET /api/idp/operations/{id}` renders pending / committed(+audit owed) / aborted / outcome_unknown; typed Re-send only after a 404, bound to the same operationId + candidate digest; typed Abandon). Secrets are write-only (body once; undefined = keep, "" = explicit clear), never in URL/marker/storage/summary; an unproven 2xx closes the dialog, latches the page and reads back once. Every mutation is `RoleAdmin`. Real-binary journeys `frontend/e2e/fe6a2.spec.ts` (IDPW appliance: repair → import → cutover → edit → delete). **FE-6A.2 correction round (2026-09-13):** import fenced on the document revision + operation-identified + ledger-recorded (`idp.import`) + in the recovery-marker flow (action `import`, Recover / typed re-send of the SAME operation); the operation commits the exact secret candidate (keyed, node-local); enabled-LDAP writes cross the non-bypassable commit-time preflight (`422 preflight_failed`, typed step + reason, zero mutation); a completed cutover is boot-idempotent (no new retirement audit); journeys W2 (fenced import), W3a (preflight refusal) and W3 (cutover through a reachable stub directory) run against the harness's in-process LDAP stub. **Round 3 (2026-09-16):** the import carries and is proven by the REVIEWED source token (`importSourceRevision`, keyed + non-disclosing; stale ⇒ `409 import_source_stale`, absent ⇒ `428 import_source_required`); the candidate key is durable/exclusive and never re-minted; boot reconciliation is a state machine with a persist-first operation-keyed audit. Remaining: GAP-2; cleartext secrets at rest (FE-6F class). |
| FE-V28 | `certificates` | `/api/ca-cert`, `/api/ca/download`, `/api/certs/upload` | admin | yes | cert upload (**no confirm today** — add one) | yes | — | E (CAPanelShowsRoot) | M — file upload + PEM textareas + OS-instructions modal |
| FE-V29 | `ca-mgmt` | `/api/ca/{status,rotate,cache-clear,key-provider}`, `/api/ocsp` | admin | yes | rotate root (**2P** token ceremony), clear cache | yes | on-view | A | **H** — two-phase rotate must be preserved exactly |
| FE-V30 | `cluster` | `/api/cluster/*` (status, mode, ha, ha/promote, tokens, revoke, revocations, drain, labels, metrics, bandwidth, node-groups, rate-limits, rotation, convergence, audit, ca) | admin | yes | enable CP (**T3**), enable HA (**T3**), promote (**T3**), CA import (**T3**), drain, revoke, token delete | yes | tick | A | **H** — 4 of 9 tier-3 ceremonies live here; enroll modal token display |
| FE-V31 | `upstream` | `/api/upstream{,/health,/settings}`, `/api/upstream/entries{,/{id},/{id}/credential}` | viewer (**mutations admin-only**) | yes | delete entry (entry-revision fenced; 409 `credential_present` while sealed material exists), **T3** credential clear (`confirm` = exact entry id), T2 credential replace (password once, body only) | yes | tick | A, E (upstream-2ff / 2ff-c / 2fd, network-2fg) | ~~L~~ **MIGRATED (2F-F, Batch 2, 2026-09-07; 2F-G closure 2026-09-08)**: `/app/network/upstream`. Viewer and operator READ; every write — create, update, delete, credential replace/clear, manual probe — is `RoleAdmin` in `ui_routes_meta.go` (the pre-2F "operator" cell here was a documentation error against the backend truth, corrected in 2F-G). Credential-free read model through fail-closed decoders, document-revision fenced create, entry-revision fenced update/delete/credential, bounded structured refusals rendered from typed allowlisted facts, an unverifiable 2xx classified UNPROVEN (latch + authoritative read-back, never retried), `yaml`-sourced entries read-only (409 `yaml_owned`; proven on a config.yaml-seeded appliance), direct-fallback banner preserved, NO client-side persistence. |
| FE-V32 | `pac` | `/api/pac/{profiles,profiles/{id}/lifecycle,pools,simulate,analyze,posture/{diff,inventory,exceptions}}`, `/api/pac-config`, `/proxy.pac`, `/pac/` | viewer (**mutations admin-only**) | yes | publish a DIRECT-capable profile (**T3**: the server-bound challenge `<profileId>:<candidateSpecDigest[0:8]>` — the pre-2F "word = profile id" form is no longer accepted), roll back, delete profile/pool, clear governance | yes | — | E (pac-2fe / 2fe-c / c2 / c3, network-2fg), Go conformance | ~~**H**~~ **MIGRATED (2F-E, Batch 2, 2026-09-05, corrected rounds 1–8; 2F-G closure 2026-09-08)**: `/app/network/pac`. Viewer and operator READ; every write — profile/pool CRUD, lifecycle save-draft/publish/rollback, DIRECT-exception governance, legacy PAC config — is `RoleAdmin` in `ui_routes_meta.go` (the pre-2F "operator" cell here was a documentation error against the backend truth, corrected in 2F-G). Node-local draft → publish → history lifecycle with a durable non-secret recovery marker (operation id + history incarnation), recovery classification from the lifecycle GET's own evidence limits, candidate-bound DIRECT challenge, and node-local labels on the draft, history and exception governance. |
| FE-V33 | `releases` | `/api/releases{,/current,/dispatch,/dispatch/status,/dispatch/resume,/catalog-refresh}`, `/api/backups` | viewer (dispatch admin) | yes | **dispatch upgrade — currently NO confirm; add ceremony**; catalog refresh | partial (dispatch audits via service) | 2.5 s recursive poll while dispatched | G (release_gui), A | **H** — async op_id lifecycle, resume, degraded `available:false` states, agent-unknown 404 copy |
| FE-V34 | `diagnostics` | `/api/diagnostics`, `/api/diagnose/{all,cluster,config,dns,etcd,storage,support,tls,upstream}` | admin (contract viewer) | diagnose verbs mutate=audited | — | yes | on-view | E (DiagnosticsPanelRuns) | M — versioned typed results; `operator_action` rendering |
| FE-V35 | `support` | `/api/support/*` (status, bundles + per-bundle approve/upload/validate/manifest/exports/redaction-report/download-sealed/download-encrypted, recipients, retention, debug-level, tac-trust, telemetry/{config,preview}, upload/config, uploads), `/api/backups` | viewer (ops operator/admin) | yes | delete bundle, approve, recipient rotate/remove (**native prompts today — replace**), passphrase entry (**native prompt today — replace**) | yes | tick (support section) | G (support_upload), A | **H** — largest workflow surface; sealed/encrypted downloads via Blob; consent gates |
| FE-V36 | `settings` | `/api/settings{,/network,/log-level}`, `/api/session-{secret,timeout}`, `/api/ui-allow-ips`, `/api/syslog`, `/api/logger`, `/api/metrics-config`, `/api/otlp`, `/api/geoip`, `/api/blockpage`, `/api/connlimit`, `/api/alerts/webhooks{,/test,/history}`, `/api/config/{export,import,versions,diff}` | admin | yes | session-key rotate (**T3**), IP allowlist (**T3**), config import replace (dry-run→confirm), version rollback (confirm; partial-failure + `runtime_only_surfaces` states), webhook delete | yes | 15 s delivery-history poll (view-gated) | E (ConfigVersionRollback), A | **H** — monolith slated for decomposition (IA §5); export/import/rollback truth-telling per config-surface registry |
| FE-V37 | `users` (Administrators) | `/api/auth/{users,lockouts,change-password}` | admin | yes | delete user (revokes sessions), clear lockouts | yes | — | E (RBACNavGating), A | M — no TOTP enrollment UI exists (backend gap GAP-2) **FE-6A.1 (2026-09-12): READ surface migrated** — `/app/administrators` (Administration; admin = uiRoutes `GET /api/auth/users` / `GET /api/auth/lockouts`; a viewer/operator deep link renders the server's 403 as a bounded state and the post-login route intent resolves to Overview). Read-only by directive: Refresh is the only control. Renders the node-local roster (authoritative username, durable role, per-user `securityGeneration` — the session-binding fact — TOTP PRESENCE only), the roster revision, the derived administrator count and last-admin posture (exactly one admin ⇒ the appliance refuses demoting/deleting it), and the INDEPENDENT node-local lock set (tier/username/source IP/seconds remaining + lock-set generation; a failed lockouts read never blanks the roster and never reads as "no lockouts"). Decoders (`src/api/admins.ts`) refuse `pass_hash`/`totp_secret`/`backup_codes`/`password`/`totp_last_counter` (any spelling) and unknown roles. **Recorded backend truth gaps (not patched with copy):** `GET /api/auth/users` carries neither a roster persistence posture (writes learn it only as `persistence_not_configured`) nor the legacy single-user mirror identity — neither is rendered. **SHIPPED (FE-6A.2, 2026-09-13):** create / role change / password set under the roster revision (query fence; T2 review; `last_admin` structured refusal), delete = T3 typed username + roster revision, lockout clear = T2 under the lock-set generation, self-service password change from the shell for every role (current credential + the caller's `securityGeneration`, now on `/api/auth/status`); results are action-bound (identity, revision, `persisted`, `sessionsRevoked`/`selfAffected`), refusals are endpoint-specific allowlists (`user_exists`, `not_found`, `stale`, `precondition_required`, `persistence_not_configured`, `persist_failed`, `last_admin`, `invalid_credentials`) and an unknown code is never a verdict; `selfAffected:true` completes the auth teardown to the login boundary; a lost/unproven response claims no revocation. Real-binary journeys `frontend/e2e/fe6a2.spec.ts` (AUTH: create → role → lockout clear → self password change → delete). GAP-2 (TOTP enrollment) unchanged; the roster read model still states no persistence posture (writes carry `persisted` / `persistence_not_configured`). |
| FE-V38 | `governance` | `/api/governance/control-plane` | admin | no | — | — | on-view | E (GovernancePanel), A | L |

## B. Cross-cutting workflows (not views)

| ID | Workflow | Endpoints | Notes | Risk |
|---|---|---|---|---|
| FE-X01 | Login / logout / session expiry | `/api/auth/{login,logout,status}` | 2-step TOTP in-band (`totp_required`); 401-anywhere → login overlay; logout revokes; **pre-auth TLS-fallback warning banner** driven by `ui_tls_fallback`/`ui_tls_fallback_reason` on `/api/auth/status` (added on main 2026-08-22) | **H** |
| FE-X02 | First-admin setup | `/api/setup/{status,complete}` | Bootstrap role injection window; complexity mirror; auto-login; **pre-auth TLS-fallback warning banner** driven by `ui_tls_fallback` on `/api/setup/status`. **Setup-time unauth/open mode (`{unauth:true}`) is WITHHELD from the v2 UI (FE-3 decision, 2026-08-22)**: the backend marks the appliance configured (`defaultAuthOutcome=Exempt` ⇒ `IsConfigured()` true, `store.go:596-600`) WITHOUT creating any in-band management identity — `uiAuthMiddleware` then requires credentials no roster can satisfy (`ui_middleware.go:254-296`, `VerifyUIUser` `store.go:943-965`); recovery requires appliance-shell/OOB credentials (`-user/-pass` flags, `auth.user`/`auth.pass` YAML via `auth_startup.go`, or `--reset-password`, `main.go:297`). The legacy browser wizard never exposed this path either (`static/index.html:18560-18580` is credential-only). Backend gap tracked as GAP-9 (SETUP-OPEN-MODE) in FRONTEND-CURRENT-STATE.md — a backend/product decision for later review, not an FE fix. The governed post-setup path (Authentication panel default-auth-outcome, with real credentials existing) is unaffected. | **H** |
| FE-X03 | Theme | `localStorage['culvert-theme']` | Only browser storage in the app — keep as the only persisted client state | L |
| FE-X04 | SSE liveness | `/api/events` | **DISPOSITIONED — intentionally NOT consumed by v2 (FE-4 decision, 2026-08-22, ADR-FE-002 Accepted)**: the CULVERT Monitor is query-driven (snapshots + bounded server-side queries, explicit refresh, visible freshness), so the v2 client opens no `EventSource` and issues no `/api/events` request — browser-proven (zero `/api/events` traffic across every FE-4 flow). The backend SSE surface is RETAINED untouched and still serves the legacy UI; this is an approved product/scale decision, not lost parity. The legacy behaviors listed here (handshake, backoff+jitter, retry cap, LIVE/STALE pill, 503-at-cap) retire with the legacy UI. | ~~H~~ closed |
| FE-X05 | Global confirm/danger/prompt dialogs | — | 3-tier system + MCP danger dialog; typed words; stack, focus trap, Esc; replace 6 legacy modals + 5 native prompt/confirm sites | **H** |
| FE-X06 | Object references ("where used") | `/api/objects/references` | Generic consumer rendering per DESIGN-SYSTEM §3 contract. **MIGRATED (2A, Batch 2, 2026-08-22)**: ONE reusable read-only `WhereUsed` consumer — explicit-interest fetch (never per-row), fail-closed decoder, and an explicit reviewed consumer→route mapping (access-rule/policy deep-links by stable ID; auth-rule and category-group render as information until their surfaces migrate; server `view` strings can never mint a client route — component-test pinned). Proven in-browser against real reference data. Future slices extend the mapping as real v2 routes land. | ~~M~~ closed |
| FE-X07 | Downloads/exports | `/api/config/export`, `/api/export`, `/api/ca-cert`, `/api/ca/download`, support downloads, `/proxy.pac` | Blob + `a.download` and direct-href patterns; per-section filenames | M |
| FE-X08 | Role-gated navigation | `/api/auth/status` | 123 `data-min-role` hides + section gating; new app: route guards + nav filtering, server still authoritative | M |
| FE-X09 | Keyboard shortcuts + a11y floor | — | 1–8 view jumps, Esc, `[data-click][role=button]` keydown; extend per UX-PRINCIPLES §12 | M |
| FE-X10 | Rule-ID deep links (traffic→policy, audit filter) | — | Currently no URL routing outside MCP; new router makes these real URLs | M |

## C. Capabilities that must not silently disappear (checklist extras)

- Multi-admin draft actor warning ("opened by <other>") on the policy draft bar.
- Staged-reorder version fencing (silently discards on rulebase movement) and
  `polBlockWhileStaged` mutation blocking.
- Shadowed-rule warnings surfaced at commit time (`st.shadows`).
- Release dispatch: `pre_backup` + `passphrase_ref` (env-ref, never a secret), idempotency key,
  forced status polling, resume-by-`op_id`, "Unknown maintenance agent" 404 copy.
- Support bundle: approve-before-download lifecycle, sealed vs encrypted download variants,
  redaction report, TAC trust, telemetry consent preview.
- Rollback response rendering: dry-run, partial "applied-but-not-persisted", and
  `runtime_only_surfaces` disclosure.
- Export redaction disclosure (webhooks/upstream secrets excluded by construction).
- MCP: ticket/supersede handling, "state is UNKNOWN" network-failure copy, `production_locked`
  and `self_approval` error surfacing, read-only Management posture, candidate-never-persisted
  guarantee.
- Decryption-profile cert-verification option set locked to the runtime enum (re-express
  `decryptprofile_cert_contract_test.go` against the new source of options).
- SSE server contract handling: 503 + `Retry-After` at cap; eviction-close reconnect.
- IdP secret fields: write-only, cleared only when explicitly checked; redaction-safe SAML
  metadata indicator.
- Blocklist feed remove: second confirm for "also remove its entries".
- Two-phase Root-CA rotation token ceremony.
- Pre-auth TLS-fallback warning banners (setup overlay, login overlay, and in-app) driven by
  `ui_tls_fallback`/`ui_tls_fallback_reason` on `/api/setup/status` and `/api/auth/status`
  (main, 2026-08-22 — the credentials-over-plain-HTTP warning must not be lost in migration).
- Security-panel scan-saturation tiles (`stat_clam_saturated`, `scan_inflight`,
  `stat_scan_late_discard` on `/api/security-scan/status`; main, 2026-08-22).
- CA cert-upload persistence disclosure: the MITM-target upload is persisted to the CA bundle
  path (label + confirmation copy fixed on main, 2026-08-22); the UI-target upload is
  validate-only.
- Country flags via Unicode regional indicators (no CDN), theme-aware charts,
  reduced-motion-safe `chart.update('none')` behavior.

## D. Known current-state defects that migration should fix, not reproduce

(From `FRONTEND-CURRENT-STATE.md` GAP table and design-doc backlog: `mcp-rollout` missing
`viewMeta`; dead `'4': 'log'` shortcut; `describeLogPersistence` SVG-in-textContent branch;
6 non-stack modals; 5 native dialogs incl. passphrase prompt; single-entry leave-guard;
background-tab polling; SSE permanent give-up; no dispatch confirm; no cert-upload confirm;
1,999 inline styles / `style-src 'unsafe-inline'`; uncached 4 MB logo.)
